# T2 — Independent review of T1 DEC-35 amendment draft

**Task:** T2, independent reviewer. **Subject:** `softhouse/T1-dec35-draft:.softhouse/handoff/20260721-dec35/T1.md`
(a draft-only migration map; no requirements document was edited).
**Method:** read the T1 handoff via `git show`; re-derived every line number, census, cross-reference,
and gate token independently against `idea-lab/final_requirements/*.md` in this worktree; read
`.softhouse/verify-docs.sh` line by line rather than trusting the prior-handoff summary the author relied on.

## VERDICT: MICRO-FIX

The map is substantively correct, matches the ratified direction, is mechanically applyable as written,
and does not regress the gate. One honesty gap: applying the map lands the personal name "Ц. Бат" into
`03_acceptance_criteria.md` (at 476 and 486) while `03:10` still asserts "No placeholder personal names
are used." That self-contradiction is a direct, non-optional consequence of the ratified amendment, so the
reconciling clause belongs **in** the map, not deferred to a downstream reviewer/PO call. Add it as map
item [6]. Everything else is APPROVE-grade.

---

## What I checked and what I found

### 1. Matches the ratified direction — PASS
- Display form is the short form "Ц. Бат" (patronymic initial + given name), `ner` shown in full. ✓
- Anti-fishing defence is explicitly **relocated from name-masking to a per-sender recipient-lookup
  velocity cap**, in DEC-35's amended Rationale, the new EP-4 bullet, and the `04:810` `429` case. ✓
- The cap is a **US-12.5 config seed with NO number** — "no hard-coded value / PO to confirm" in every
  place it appears (items [1], [4], [5]). No integer invented. ✓
- No masking is quietly retained and no new masking scheme is invented. ✓

### 2. Census completeness — INDEPENDENTLY CONFIRMED (this was the critical check)
I re-ran the census from scratch over all seven docs:
- `grep -niE 'last initial|first name|last name'` → **exactly 2** assertions of the old rule: `05:159`
  (DEC-35) and `03:476` ([PROPOSED] line). Matches the author's claim.
- `grep -nE 'Member [A-Z]\.'` → the only rendering *shown to a member* in P2P confirmation is `03:486`.
  `03:10` is the actor convention; `03:874` ("retrievable by Member A.") is sentence-final actor prose,
  a false positive. Matches the author's claim of **exactly 1** display string.
- Prose blind-spot sweep (`mask|surname|family name|initial|abbreviat|shows .*name|displays? .*name|payee`
  across all docs, plus a walk of every EP-4 / US-4.x / US-11 scenario): **no other prose expresses the
  old first-name/last-initial name-model.** The generic "recipient display name for confirmation" lines
  (`01:273` F-112, `04:84`, `04:222`, `03:472`, `02:174`, `05:65`) state no masking form and correctly
  require no edit. `04:109` ("first/last, no address") is inside the superseded-drafts adjudication table
  and is explicitly overridden by DEC-6, not an active assertion.
- The project's characteristic failure (a name-model assumption in PROSE the token check can't see) is
  **not present here** — the author caught the one prose occurrence (`03:486`) that the previous run
  shipped past. This is the finding that would have mattered most, and it is clean.

The author's "Lines deliberately NOT in map" list omits `02:174` and `05:65`, but both are generic
display-name lines asserting no masking form, so nothing is left behind. Trivial documentation omission,
not a defect.

### 3. Mechanical applicability — PASS
Every item is file / line / exact current text / exact replacement, applyable without re-derivation.
I verified each against the live files:
- `05:159` DEC-35 row — quoted "current" matches byte-for-byte. Header at `05:145` matches. ✓
- `03:476` [PROPOSED] line — matches. ✓
- `03:486` US-4.1 Scenario 1 — matches. ✓
- Item [4] insertion point: `03:474` is the DEC-33 send-velocity seed, `03:475` is the SLA line —
  inserting the lookup-velocity bullet between them is correct and reads as siblings. ✓
- `04:810` recipient-lookup row — matches. ✓
- Cross-refs in the replacement text all resolve: US-4.1 Scenario 2 (`03:491`) *is* the uniform-response
  enumeration scenario; `03:368` *is* the enumeration-protection convention; `04 §3.4` (header `04:806`)
  *is* the EP-4 payments section containing `04:810`; DEC-33 *is* at `05:157`, EP-4, send velocity — so
  the "caps sends vs caps lookups" distinction is well-founded (DEC-33 send-limit is illustrated at
  US-4.1 Scenario 3, `03:498`). ✓
- `429 REMINDER_THROTTLED` precedent for the proposed `429 LOOKUP_RATE_LIMITED` exists at `04:822`. ✓

### 4. The 03:10 tension — MICRO-FIX (the one real finding)
`03:10`: "…No placeholder personal names are used." Item [3] keeps "Member B" as the actor and frames
"Ц. Бат" as *an example of Member B's registered name*. That framing cleanly resolves the **actor-rename**
concern — but it does **not** resolve the **placeholder-personal-name** concern: "Ц. Бат" is
unambiguously a personal name, and after the edit it appears in doc 03 at both `476` and `486`, directly
falsifying the blanket sentence at `03:10`. The framing is therefore *not fully clean*.

Two facts make the honest fix the carve-out, not the framing:
- The ratified direction **requires** the short form to appear in the acceptance criteria, so the personal
  name enters doc 03 necessarily — this is not an optional stylistic choice a reviewer can wave through.
- **`03:10`'s blanket claim is already stale**: `03:45` uses Cyrillic personal-name field values
  (`ner` "Болд", `etsgiin_ner` "Батын") introduced by the DEC-6 migration. The author did not surface
  this precedent; it both weakens the "no placeholder names" absolute and shows the doc already tolerates
  illustrative Cyrillic name examples.

**Correct fix (add as map item [6]):** a one-clause carve-out at `03:10`, e.g. append to the Actors
bullet: "Illustrative Cyrillic name values (e.g. DEC-6 field examples, and the DEC-35 recipient short-form
'Ц. Бат') are format examples, not actor identities." This makes the applied document internally
consistent. Severity LOW (single clause), but it must be *in* the map because applying the map without it
produces a self-contradicting document. The author correctly identified the tension (D4/Follow-up 2) but
wrongly parked the fix downstream.

### 5. Right-sized — PASS (with one observation)
No new DEC; extend DEC-35 + one EP-4 rule + a `04:810` note. Defensible and minimal: the cap is a US-12.5
config seed exactly like DEC-33's, the PO ratified display+relocation as one decision, and the
enumeration protection it complements already exists. Not over-reaching — the proposed `429` code is the
only genuinely new artifact and is properly flagged as a proposal with in-file precedent.
*Observation (not a defect):* burying the lookup-velocity control inside DEC-35's Rationale column means a
future reader searching the decision log for "recipient-lookup velocity" finds it only in prose, not as a
titled decision. Acceptable given the ratification framing, but if traceability later matters, a dedicated
DEC would be the cleaner home. Not required to approve.

### 6. No collateral change — PASS
Only DEC-35 (`05:159`) and its directly-aligned rules (`03:476`, `03:486`, new `03` bullet, `04:810`) are
touched. **DEC-3** (P2P addressing) and **DEC-33** (send velocity) are *referenced*, never modified —
the DEC-33 row at `05:157` is untouched. No other ratified DEC (DEC-1..20 in `01` §6, DEC-21..75 in
`05` §3) is altered. ✓

### 7. Gate (`.softhouse/verify-docs.sh`) — PASS, independently re-audited against the script
I read the script directly rather than trusting the prior handoff (which the author flagged UNVERIFIED).
- HARD `first_name|last_name` (underscore): no replacement string contains the underscore token. The
  prose "first name + last initial" retained in the DEC-35 *Proposed rule* column has a **space**, so it
  is not counted. Author's reasoning confirmed. ✓ (FDIC/NCUA, MNT 3m, float/DECIMAL, UTC+8 — none
  introduced.) ✓
- DRIFT `usd` (`\$[0-9][0-9,]*`): the only edited line carrying a `$`-amount is `03:486`, which
  **preserves `$25.00`** verbatim; no other replacement adds or removes a `$`-amount. Count stays 312. ✓
- DRIFT `rails` (`ACH|FedNow|SEPA`): no replacement string contains any rail token (the Cyrillic strings
  and `LOOKUP_RATE_LIMITED` match nothing). Stays 56. ✓
- DRIFT `vendor` (Stripe/Plaid/Lithic/Persona-context): none introduced. Stays 83. ✓
Applying the map would not move the gate.

### 8. Provenance — PASS
The "incumbent Mongolian bank-app convention" claim is correctly marked **`[UNVERIFIED]`** and explicitly
*not* smuggled in as fact — the author names no source and manufactures no citation (consistent with the
prior run's fabricated-source retraction). The load-bearing structural claim (`ner` is the identity, no
family name to initial) rests on `[VERIFIED: CLAUDE.md]`. The `429 LOOKUP_RATE_LIMITED` code and the
verify-docs semantics are also honestly flagged `[UNVERIFIED]`. No fabricated `[VERIFIED]` marker found.

---

## Required change before apply
1. **Add map item [6]:** one-clause carve-out at `03:10` reconciling "No placeholder personal names are
   used" with the illustrative Cyrillic examples the DEC-35 amendment (and the existing DEC-6 examples at
   `03:45`) require. Without it, applying the map yields a document that contradicts its own actor
   convention.

## Optional / downstream (not blocking)
- Consider whether the lookup-velocity control warrants its own DEC for decision-log traceability
  (currently lives inside DEC-35's Rationale).
- Confirm the proposed error code `429 LOOKUP_RATE_LIMITED` against the API error vocabulary owner.
- The census's "NOT in map" list may add `02:174` and `05:65` for completeness (no edit needed).

## Summary
Direction match ✓ · census independently confirmed (2 assertions + 1 display, no missed prose) ✓ ·
line numbers & current text exact ✓ · cross-refs resolve ✓ · no collateral DEC change ✓ · gate
re-audited, no token movement, `$25.00` preserved ✓ · provenance honest ✓. **One MICRO-FIX:** fold the
`03:10` carve-out into the map so the applied document is internally consistent.
