# T4 — Independent review of the applied DEC-35 amendment (last review before merge)

**Subject:** `softhouse/T3-apply-dec35` — applies the T1 map (5 items, APPROVED) + the reviewer-mandated
MICRO-FIX (map item [6]) amending DEC-35 (P2P recipient-confirmation display).
**Method:** read all three prior handoffs via `git show`; re-derived the diff `main...softhouse/T3-apply-dec35`
independently; byte-compared applied text against the T1 map; re-audited the 429 precedent, the old-rule
census, and the DEC tables against the T3 branch content; overlaid the T3 doc versions into the working
tree and ran `.softhouse/verify-docs.sh` myself. Assumed defects existed and looked for them.

## VERDICT: APPROVED

No defect found. Every map item is applied — verbatim where the map gave exact text — the one deviation
(the 429 code) is a correct, disclosed precedent-following override, the cross-document rule is now single
and consistent, scope held, the gate passes at baseline, and provenance is honest. Detail below.

---

## Findings by check

### 1. Every map item applied, verbatim where exact — PASS
- **Item [1]** DEC-35 row (`05:159`): applied row is **byte-for-byte identical** to the T1 map replacement
  (`diff` clean, both 1076 bytes). Status CONFIRM→AMEND; Proposed-rule column preserved as the historical
  proposal per table convention.
- **Items [2] (03:476), [3] (03:486), [4] (new 03 EP-4 bullet)**: applied text matches the T1 map exact
  replacements word-for-word (verified against the diff). Item [4] inserted between the DEC-33 send-velocity
  seed and the SLA line, as the map specified — the two velocity controls read as siblings.
- **Item [5] (04:810)**: display-note half matches T1 exactly; the **one deliberate deviation** is the error
  code `429 LOOKUP_THROTTLED` in place of T1's `429 LOOKUP_RATE_LIMITED`.
- **Item [6] (03:10 carve-out)**: applied as the reviewer-mandated MICRO-FIX (see check 4).
- **429 override verified — correct, not a silent deviation.** T3's cited precedent holds under my own grep
  of `04_technical_architecture.md`: per-row 429 codes are `<X>_THROTTLED` (`429 REMINDER_THROTTLED` at
  04:822, `429 REPORT_THROTTLED` at 04:893); the only `RATE_LIMITED` token is the **bare generic common
  code** `429 RATE_LIMITED` (04:751, no prefix); there is **no prefixed `<X>_RATE_LIMITED` anywhere**. So the
  established per-row convention is `_THROTTLED`, and this is precisely the form T2's naming note recommended.
  The override is disclosed in T3 §Decisions (D-name) and flagged `[UNVERIFIED]` as a proposal. Correct.
- Nothing improvised beyond this; no STOP condition was hit; all "current" strings matched at cited lines.

### 2. Cross-document consistency (highest-value check) — PASS
The display rule now reads as ONE rule across DEC-35 (`05:159` Final value/Rationale), the adjudicated
[PROPOSED] line (`03:476`), the happy-path scenario (`03:486`), and the endpoint (`04:810`): "standard
Mongolian short form = patronymic initial + given name (Ц. Бат), `ner` shown in full; anti-fishing moved to
the lookup-velocity cap." I swept the whole corpus (all 7 docs) for surviving expressions of the old rule
(`last initial`, `first name +`, `Member [A-Z]\.`):
- `05:159` — "first name + last initial" survives **only in the Proposed-rule (proposal) column**, which
  legitimately preserves the original per table convention (same pattern as DEC-26/28/37 rows). The LIVE
  rule lives in Final value/Rationale and is the new one. **OK, not a contradiction.**
- `03:875` — "retrievable by Member A." is sentence-final actor prose (false positive). **OK.**
- No other occurrence anywhere. The one prose display string the previous run had shipped past (`03:486`)
  is now converted. No place still implies "first name + last initial" as the live rule.

### 3. Lookup-velocity cap — PASS
Carries **no hard-coded number** in any of its three appearances (DEC-35 Rationale, the new `03` EP-4 bullet,
the `04:810` 429 case): "recipient-lookups per sender per period", "not hard-coded", "no threshold is defined
here (PO to confirm)", "US-12.5 config seed". It references DEC-33 and the existing uniform-response
enumeration protection as complements — "DEC-33 caps sends, this caps lookups" — and does **not** modify or
duplicate them (DEC-33 row at `05:157` is untouched; the enumeration protection is referenced, not recreated).

### 4. The `03:10` carve-out — PASS (reconciles both, minimal)
Applied text: "No placeholder personal names are used **as actor identities; illustrative Cyrillic name
*values* (e.g. DEC-6 field examples `ner` "Болд", and the DEC-35 recipient short-form display "Ц. Бат") are
format examples, not actors.**" It names **both** sources: the DEC-6 field examples (confirmed live at
`03:45`: `ner` "Болд", `etsgiin_ner` "Батын" — the clause cites the DEC-6 field-examples class, covering
both) **and** the DEC-35 "Ц. Бат". It is a single appended clause, not a rewrite. Matches the intent and
form of T2's mandated item [6]. **OK.**

### 5. Scope — PASS
Only four paths changed: the T3 handoff plus `03`, `04`, `05`. DEC-35 amended **in place** (still "DEC-35",
not renumbered); header/ID/traceability intact. No currency, rails, vendor, name-model, DEC-3 or DEC-33
change — those are referenced, never modified. `00`, `01`, `02`, `06` untouched.

### 6. No other ratified DEC changed — PASS
`01_business_analysis.md` (DEC-1..20, §6) is not in the diff at all. In `05` §3 (DEC-21..75) the **only**
changed DEC row is DEC-35 (line 159); DEC-32/33/34/36/37/38 and all others are byte-unchanged.

### 7. Gate — PASS (ran it myself)
Overlaid the T3 doc versions and ran `.softhouse/verify-docs.sh`:
```
HARD:  FDIC/NCUA 0 · MNT3m 0 · first_name/last_name 0 · float/DECIMAL 0 · UTC+8 0
DRIFT: usd 312 (= baseline) · rails 56 (= baseline) · vendor 83 (= baseline)
VERDICT: PASS   (exit 0)
```
All HARD 0; all DRIFT = baseline (usd=312, rails=56, vendor=83 per `.softhouse/baseline.txt`). Confirmed the
`$25.00` at `03:486` survived — it is preserved verbatim, which is what keeps `usd` at 312. Working tree
restored after the run (`git checkout --`).

### 8. Provenance — PASS
Real markers throughout the T3 handoff. The "incumbent Mongolian bank-app convention" claim remains
`[UNVERIFIED]` and is not smuggled in as fact; the load-bearing structural claim (`ner` is the identity, no
family name to initial) rests on `[VERIFIED: CLAUDE.md three-field non-negotiable]`. The `429 LOOKUP_THROTTLED`
code is honestly flagged `[UNVERIFIED]` as a proposal. No fabricated `[VERIFIED]` marker found.

---

## Nits (non-blocking, no change required)
- The lookup-velocity control lives inside DEC-35's Rationale prose rather than as a titled decision — a
  future reader searching the decision log for "recipient-lookup velocity" finds it only in prose. T2 already
  raised this as an optional/downstream observation; acceptable given the PO ratified display+relocation as
  one decision. Not a defect.
- `429 LOOKUP_THROTTLED` should still be confirmed with the API error-vocabulary owner (already in T3
  follow-ups). Proposal, not citation — correctly labelled.

## What I checked (summary)
Map fidelity (byte-compared item [1]; verified [2]-[6] against T1) ✓ · 429 precedent re-grepped ✓ ·
cross-doc rule single & consistent, corpus-wide old-rule sweep ✓ · velocity cap numberless, references not
duplications ✓ · carve-out reconciles DEC-6 + DEC-35, minimal ✓ · scope = DEC-35 + aligned rules only,
amended in place ✓ · no other DEC touched (01 §6 absent, only DEC-35 row in 05 §3) ✓ · gate run myself:
PASS, HARD all 0, DRIFT = baseline, `$25.00` survived ✓ · provenance honest, incumbent-convention claim
`[UNVERIFIED]` ✓.

**Recommendation: APPROVED for merge.**
