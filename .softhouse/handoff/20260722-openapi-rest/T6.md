# T6 — Independent review: T2 (admin/compliance) + T3 (entity-gated) OpenAPI slices

Reviewer: T6 (independent). Scope: OpenAPI CONTRACT YAML only; no application logic reviewed or written.
Branches reviewed on top of merged main (foundation + EP-1..EP-4 + shared schemas):

- `softhouse/T2-openapi-admin-compliance` — EP-11 (§3.11), EP-12 (§3.12), EP-13 (§3.13)
- `softhouse/T3-openapi-entity-gated` — EP-5 (§3.5), EP-6 (§3.6), EP-10 (§3.10)

## VERDICT: APPROVED

Both slices are correct against `04` and the project non-negotiables. The T2+T3+foundation
assembly validates clean, with no duplicate path/schema and no unresolved `$ref`.

## Assembly + validation (MUST-DO)

Copied both branches' `paths/`, `schemas/`, `params/` into the review worktree and ran
`python3 backend/openapi/validate.py`:

```
OpenAPI: 132 paths · 147 operations · 220 schemas
VERDICT: PASS
```

- Duplicate schema: NONE. Duplicate path: NONE. Unresolved `$ref`: NONE (validator's
  local-ref check passed; it exits 1 on any of these).
- This is the T2+T3-specific assembly (foundation + EP-1..4 + EP-5,6,10,11,12,13). EP-7/8/9
  are absent here, so the AdminBallot-vs-EP-8 clash cannot be exercised in THIS assembly — but
  the T2 slice pre-empts it by name-prefixing (below), and the full 4-way assembly (183 ops)
  is reported to validate.

## Check-by-check

1. **Coverage** — PASS. Every row of §3.5/§3.6/§3.10 (T3) and §3.11/§3.12/§3.13 (T2) maps to an
   operation. T3: 38 ops (EP-5 9 rows→10, EP-6 20 rows→25, EP-10 2 rows→3). T2: 45 ops
   (EP-11 8, EP-12 23, EP-13 14). Only the two legitimate T2 exclusions
   (`/admin/members/{id}/status-transitions` → EP-2; dividend-run admin → EP-7 gap-fill). No
   other omission. (Note: T3 handoff prose says "EP-5 8 rows"; the table actually has 9 rows —
   a prose miscount only; all operations are present.)

2. **Entity-gating (T3, highest priority)** — PASS. All 38 EP-5/EP-6/EP-10 operations carry
   `x-entity-gated: true` + `x-entity-gated-reason` + a description note. Mechanically verified:
   per file `operationId` count == `x-entity-gated: true` count (EP-5 10/10, EP-6 25/25,
   EP-10 3/3). None unmarked. T3's 38/38 claim is accurate.

3. **BLOCKED material not encoded (T3)** — PASS. Zero APR / `_bps` / interest-rate /
   computed-installment FIELDS in EP-6; every such token appears only in comments explaining the
   omission. All amounts (`principal_due`, `interest_due`, `accrued_interest`, `payoff_amount`,
   `repayment_estimate`, …) are `$ref '#/components/schemas/Money'`. No `type: number` / float
   anywhere in EP-5/6/10. EP-10 encodes NO round-up denomination; `RoundUpMultiplier` is the
   E-47 `multiplier` enum {1,2,3} (verbatim), correctly distinguished from the deferred
   "nearest $1/$2/$5" denomination.

4. **Staff auth (T2)** — PASS. All 23 EP-12 ops override to `staffSSO`; no member OAuth2 leak.
   EP-13 admin ops (aml rules/alerts, report runs, catalog, audit-log, data-request admin) use
   `staffSSO`; member-facing ops (capital-ledger + category drill-down, impact-scorecard,
   data-requests create/get) inherit the default `memberOAuth2`. EP-11 admin catalog uses
   `staffSSO`; member notification ops inherit `memberOAuth2`. Maker-checker noted on EP-12
   mutations (23 references; `approval_id`/`MakerCheckerApproval` modeled).

5. **No shared schema redefined; collision guard** — PASS. T2 prefixes governance/loan admin
   schemas `Admin*` (AdminBallot, AdminBallotType, AdminCandidate, AdminProposalQueueResponse,
   AdminLoanDecision, AdminRestructureOrigin, AdminPledgeAction, …) to avoid EP-8/EP-4 clashes.
   T3's `LoanScheduleType` deliberately avoids EP-4's `ScheduleType` (both present in this
   assembly, no collision); `LoanStatus` is new (single definition). Assembly duplicate-check:
   clean.

6. **Step-up (T3)** — PASS. `stepUpAssertion` is required (composed with `memberOAuth2`) on PAN /
   card-credential reveal (`revealCardCredentials`), PIN set (`setCardPin`), and `activateCard`
   (04 §3.5 marks the row step-up) — EP-5: 3. EP-6 step-up rows also carry it: `acceptLoanOffer`,
   `createPeerGuaranteePledge`, `createSignatureSession`, `updateLoanAutopay` — EP-6: 4. Total 7,
   matching the handoff. Idempotency-Key on the two money-movement POSTs
   (`makeLoanPayment`, `createPeerGuaranteePledge`) via the shared param.

7. **Honesty** — PASS. Enums verbatim: `LoanStatus` == DEC-20 exactly
   (DRAFT|SUBMITTED|UNDER_REVIEW|APPROVED|ACTIVE|DELINQUENT|PAID_OFF|DEFAULTED|WRITTEN_OFF);
   `StaffRole` == §2.10 exactly (…|SUPER_ADMIN). Inferred fields carry INFERRED notes.
   `ConfigurationParameter.value` is schema-open with NO baked seed constant/default/enum; the
   `key` field lists namespace examples (`share.par_value_cents`, …) as key NAMES, not values —
   the non-seed rule holds. Deposit non-negotiable honored: "insured/protected/guaranteed" occurs
   only as explicit negations in the Capital Ledger schema + path.

## Observations (non-blocking, not fixes)

- EP-12's lending-admin ops (referrals, decisions, restructures, pledge actions, collections,
  write-off) sit under the staff console but operate on entity-gated EP-6. They carry a prose
  "EP-6 entity-gated (CLAUDE.md); contract only" note rather than the `x-entity-gated: true`
  extension. Acceptable — the mandated machine tag is a T3 (EP-5/6/10) rule; T2's prose note keeps
  the gating visible on the admin side. Worth a consistency pass if EP-12 is ever mechanically
  filtered by the gating tag.
- Vendor leaks (Lithic / `502 ISSUER_UNAVAILABLE`, FCM/APNs, Persona evidence packs) are preserved
  verbatim from `04` with "vendor assumption / pending requirements change" notes — correct per
  CLAUDE.md (do not invent a Mongolian substitute in the contract).

no application logic written
