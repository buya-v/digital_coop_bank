# T7 — Independent review: ORM governance/funding (T4) + platform (T5)

Branch: `softhouse/T7-review-gov-platform`
Reviewer task T7. Scope: SQLAlchemy ORM **table structure** only (not app logic).
Reviewed: `softhouse/T4-orm-governance-funding` (E-34..E-46) and
`softhouse/T5-orm-platform` (E-49..E-59), both against the merged ORM foundation.

## VERDICT: APPROVED

Both slices are faithful, pattern-conformant table derivations. The gate passes
with both slices co-loaded; the secret-ballot invariant holds structurally; no
float money; no invented enum value; no baked config seed. The only follow-ups
are the two documented cross-slice bare-UUID FKs that must be promoted at merge
(one of which — `governing_ballot_id` — is now resolvable because T4 supplies the
`ballot` table). None of these is a defect in a standalone slice.

## MUST-DO gate (both slices copied into one worktree, co-loaded)

```
$ cd backend && python3 -m app.db.check_models
ORM: 29 tables · 306 columns · 17 money columns (MoneyMinor)
VERDICT: PASS   (exit 0)
```

- **29 tables** = foundation 5 (member, membership_share, account, transaction, ledger_entry) + T4 governance 9 + T4 funding 4 + T5 notifications 3 + T5 admin 4 + T5 compliance 4. No duplicate table name.
- **Float-money columns: NONE.** check_models' structural Float/Numeric scan is clean; all 17 money columns are `MoneyMinor` (BIGINT minor units).
- **Unresolved (declared) FKs: NONE.** Every `ForeignKey(...)` resolves within the co-loaded metadata.
- **Duplicate enum names: NONE that collide.** Four enum type-names are *reused* across columns (`ballot_type`, `proposal_category`, `notification_category`, `notification_channel`) — but each reuse is the **same** Python enum class under the same name, not two different types sharing a name. Benign; see integration note.

## Checks

**1. Coverage — PASS.** Every entity E-34..E-46 and E-49..E-59 is exactly one
table. Spot-checked the dense entities column-by-column against 04 §2.8–2.11:
Proposal (E-34), Ballot (E-37), CommunityProject (E-43, all 3 derived `amount_*`
+ review/updates/impact_report present), ComplianceCase (E-54), AuditLogEntry
(E-56), ConfigurationParameter (E-55), CapitalAllocationSnapshot (E-59),
SurplusMatch (E-46), CommunityGrantPool (E-45) — no dropped column. T4's only
omission is cross-slice FK hardness (below); confirmed nothing else dropped. The
E-38 `ballot_id` UQ and the composite UQs (cosignature, participation, candidate,
surplus_match, grant_pool fiscal_year, notification_preference, config key+version)
are all declared. ProxyDelegation's `UQ(delegator,scope) WHERE ACTIVE` correctly
left to an Alembic partial index, not approximated as a plain composite UQ.

**2. SECRET BALLOT (§5.1) — PASS (verified on loaded metadata).**
- `vote_record` columns: `ballot_id, choice, candidate_selections, validity_proof, voided, cast_at, id`. **No `member_id`, no member-named column.**
- `vote_record` foreign keys: exactly one — `ballot_id → ballot`. No FK to member.
- `vote_participation` carries **no choice column** (`ballot_id, member_id, delegate_member_id, via_delegation, superseded_by_direct_vote, receipt_hash, voted_at, auth_context, +timestamps`).
- Columns shared between the two tables: **only `ballot_id`** (plus each table's independent `id` PK, which is not a link). `ballot_id` is many-voters-to-one, so it enables no member↔choice join.
- `candidate_selections` is a bare `UUID[]` (no FK) → no relational path to a voter.
- `vote_record` deliberately does **not** inherit `Timestamps` (no `updated_at`); its only time field is `cast_at` (service-coarsened), mirroring LedgerEntry. Confirmed present-and-correct.
- Note (not a defect): the transitive path `vote_record→ballot→proposal.author_member_id→member` reaches only the *proposal author* (a public, single, non-voter identity), not any voter — it does not deanonymize choices.

**3. Money — PASS.** Every amount/balance/budget is `MoneyMinor`: E-45 grant-pool
`budget/committed/disbursed`; E-46 surplus-match `project_cap/accrued_amount/released_amount`;
E-59 `total_managed_funds`; plus all funding project/backing amounts. Nested
per-category amounts in E-59 `allocations` and E-43 `budget` correctly live inside
JSONB (04 models them nested), not as float columns. `match_ratio_bps` is Integer
(a ratio, not money) — correct.

**4. Honesty / enums — PASS.** Real `Enum` only where 04 lists the value set;
values verbatim (StaffRole 7, NotificationCategory 10, NotificationChannel 4,
ComplianceCaseStatus 9, VoteChoice 3, etc. — all match 04). `Proposal.rejection_reason`
is correctly `DeferredEnum` (String) — 04 says "Enum **incl.** QUORUM_NOT_MET" (open
set); a real enum here would invent values. `action_type` (E-53), `report_key`
(E-58), config `key` (E-55) are open Strings (04 gives examples only). No invented
enum value anywhere. `Notification.channel_dispatches` status set stays inside JSONB
per 04. Confirmed.

**5. No baked config-seed — PASS.** E-55 `ConfigurationParameter.value` is JSONB,
open type, no column default; no ₮ par/limit baked as a default anywhere (membership
`par_value` likewise carries no default). UQ(key, version) present.

**6. Cross-slice bare-UUID FKs — 2 to PROMOTE at merge (assembly step):**
- `community_grant_pool.funded_from_declaration_id` → **DividendDeclaration (E-32/S-7 dividend slice).** Still absent even after T4+T5 merge; stays bare UUID until the dividend slice lands, then promote to `ForeignKey("dividend_declaration.id")`.
- `configuration_parameter.governing_ballot_id` → **Ballot (E-37).** T4 supplies the `ballot` table, so once T4+T5 are on one metadata this is **immediately promotable** to `ForeignKey("ballot.id")` — the merger should wire it. (Correctly bare in the standalone T5 gate.)
- (`audit_log_entry.correlation_id` is a bare UUID by design — a tracing id, not an FK. `staff_user.staff_idp_id` is an external IdP String. Neither is a cross-slice FK.)

**7. AuditLogEntry (E-56) — PASS.** `sequence` BIGINT monotonic; `prev_hash`
(nullable, genesis) / `entry_hash` present as `String(128)`; table omits the
Timestamps mixin (WORM / append-only, no `updated_at`) — matching LedgerEntry; its
domain time is `occurred_at`. Chain computation/verification correctly NOT
implemented.

## Integration notes for the merger (T8/assembly)

- Promote the two cross-slice FKs above (governing_ballot_id now; funded_from_declaration_id when the dividend slice merges).
- Shared PG enum types (`ballot_type`, `proposal_category`, `notification_category`, `notification_channel`) are reused across tables. Structure is fine and the gate passes; at **Alembic DDL generation** ensure each native enum type is `CREATE TYPE`'d once (SQLAlchemy dedupes shared named enums, but confirm the migration doesn't emit a duplicate `CREATE TYPE`). No model change required.
- Circular FKs Ballot↔EligibilitySnapshot and Proposal↔Ballot rely on nullable columns (set at open/scheduling) — intentional and resolved by string refs.

## Blockers

None introduced by this review. Standing project blockers unchanged (E-vote quorum
legality, MNT/rail/vendor migration) — none depended on by these table definitions.

no application logic written
