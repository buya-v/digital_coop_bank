# T1 — code-sync: ORM models + initial migration + tests → migrated requirements names

Branch: `softhouse/T1-codesync-orm` (committed, not pushed).
Scope: `app/models/*.py` + `migrations/versions/0001_initial.py` (gate-coupled — renamed in
lockstep so the migration DDL still equals the ORM metadata DDL). Tests needed no changes
(no test/fixture referenced any old name).

## Renames applied (old → new + file)

### Column / field
| old | new | file(s) |
|---|---|---|
| `persona_inquiry_id` (column) | `kyc_inquiry_id` | `app/models/identity.py` (KycSubmission), `migrations/versions/0001_initial.py` (kyc_submission CREATE TABLE) |
| unique constraint `uq_kyc_submission_persona_inquiry_id` | `uq_kyc_submission_kyc_inquiry_id` | `migrations/versions/0001_initial.py` (auto-derived in the model from the naming convention `uq_%(table_name)s_%(column_0_name)s`) |
| `plaid_item_ref` (column) | `account_link_ref` | `app/models/payments.py` (ExternalAccountLink), `migrations/versions/0001_initial.py` (external_account_link CREATE TABLE) |

### Enum members + values
| old | new | file(s) |
|---|---|---|
| `VerificationMethod.PLAID_INSTANT` (value `"PLAID_INSTANT"`) | `INSTANT_LINK` (value `"INSTANT_LINK"`) | `app/models/payments.py`; migration `CREATE TYPE verification_method AS ENUM (...)` |
| `PaymentRail.ACH` (value `"ACH"`) | `ACH_PLUS` (value `"ACH_PLUS"`) | `app/models/payments.py`; migration `CREATE TYPE payment_rail AS ENUM (...)` |
| `PaymentRail.WIRE` (value `"WIRE"`) | `RTGS` (value `"RTGS"`) | same |
| `PaymentRail.RTP` (value `"RTP"`) | REMOVED (folds into `ACH_PLUS`, 24/7 — no separate real-time rail) | same |
| `PaymentRail.INTERNAL_P2P` | UNCHANGED (kept) | same |

`payment_rail` enum drops 4 → 3 members: `('INTERNAL_P2P', 'ACH_PLUS', 'RTGS')`.
`verification_method` values: `('INSTANT_LINK', 'MICRO_DEPOSIT')`.

### Unchanged per brief (already neutral)
- `processor_token_ref` — kept.
- `issuer_card_ref` — kept.

### Vendor names neutralized in comments/docstrings (role abstractions; no Mongolian vendor invented)
- `app/models/identity.py` module docstring + `kyc_inquiry_id` comment: Persona/"kept verbatim from 04, awaiting the requirements change" → "eKYC provider (procurement/TBD; ХУР/XYP compliant-alternative candidate)", "migrated to match 04 (DEC-5 / rails run)".
- `app/models/payments.py` module docstring, `ExternalAccountLink` docstring + `account_link_ref` comment, `PaymentRail` docstring: Plaid → "account-linking provider (procurement/TBD)"; ACH/WIRE/RTP market note → ACH_PLUS/RTGS + configurable Governor-set threshold; "migrated to match 04 (DEC-5 / rails run)".
- `app/models/cards.py` module docstring: Lithic → "card issuer-processor (procurement/TBD)"; NETC note retained; "migrated to match 04 (DEC-5 / rails run)". (`issuer_card_ref` identifier unchanged.)

## Gate outputs (verbatim, run from `backend/`)

```
$ python3 -m app.db.check_models
ORM: 60 tables · 677 columns · 49 money columns (MoneyMinor)
VERDICT: PASS

$ python3 -m app.db.check_migration
models: 139 DDL statements · migration: 139 DDL statements
139 statements match — migration is in lockstep with the models
VERDICT: PASS

$ python3 -m pytest -q
........................                                                 [100%]
24 passed in 0.34s
```

## Confirmations
- No float/Numeric/Decimal money column introduced; `MoneyMinor` not weakened. Money-column count unchanged at 49 (MoneyMinor). The only `Float`/`Numeric`/`Decimal` grep hits are pre-existing docstrings that FORBID float (e.g. dividends.py scaled-integer note).
- No unrelated change: diff touches exactly `app/models/{identity,payments,cards}.py` and `migrations/versions/0001_initial.py`. Tests unchanged (none referenced an old name).
- Migration and model renamed TOGETHER → check_migration PASS proves model DDL == migration DDL.

no code executed against a live DB
