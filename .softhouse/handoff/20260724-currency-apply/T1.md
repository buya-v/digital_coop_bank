# T1 — Apply USD→MNT to 01_business_analysis.md

Task T1 (APPLY, edit-only). Applied the PO-confirmed currency migration to
`idea-lab/final_requirements/01_business_analysis.md` **by role, line by line**,
per `.softhouse/currency-apply-brief.md` (single source of truth) and the T3
derivation table (`.softhouse/handoff/20260721-currency-policy/T3.md`). No value
re-derived; brief applied as confirmed. Branch `softhouse/T1-currency-01`.

**Honesty posture:** every token below is provenance-tagged [VERIFIED] against the
brief's confirmed/held tables. Nothing guessed. One out-of-scope normative item is
flagged for a coordinator decision rather than silently changed.

## Scope reality in 01
The income-anchored **limit definitions** (step-up DEC-36, velocity DEC-33, loan
bounds DEC-45, AML monitor DEC-73) live in **05** (DEC-21…75), **not in 01** — 01 §6
is normative only for DEC-1…20. Confirmed by grep: 01 contains **no** `$1,000 /
$2,000 / $5,000 / $10,000` limit tokens. Therefore in 01 the only confirmed change
is the **DEC-11 membership share par** (the keystone). Everything else in 01 is
HELD (KPI cluster + persona incomes).

## CHANGED tokens (old → new + role) — [VERIFIED against brief CONFIRMED table]
Share-par role: `$25 / $25.00` → `10,000₮` (DEC-11 / OI-1 keystone; **provisional,
pending DEC-11 / legal**). Provisional qualifier added on first use (CAP-2.1) and on
the normative DEC-11 statement.

| # | line | old | new | role / note |
|---|---|---|---|---|
| 1 | 163 (CAP-2.1) | `one initial share at $25 par` | `one initial share at 10,000₮ (provisional, pending DEC-11 / legal) par` | par role; **first use in doc** → carries provisional qualifier |
| 2 | 254 (F-103) | `($25 par value, DEC-11)` | `(10,000₮ par value, DEC-11)` | par role (onboarding-flow spec figure); plain |
| 3 | 367 (DEC-11 statement) | `at $25.00 par value` | `at 10,000₮ (provisional, pending DEC-11 / legal) par value` | par role; **normative definition** → carries provisional qualifier |
| 4 | 367 (DEC-11 rationale) | `$25 is meaningful enough…` | `10,000₮ is meaningful enough…` | par role, rationale prose; value updated |
| 5 | 367 (DEC-11 rationale) | `but $25 is the working canonical value for all documents` | `but 10,000₮ is the provisional working value for all documents pending DEC-11 / legal` | par role; false-canonical clause reconciled to provisional |
| 6 | 386 (glossary) | `dividend-bearing; par $25.` | `dividend-bearing; par 10,000₮.` | par role; plain |
| 7 | 410 (Open Item 1) | `confirmation of the $25 share par value` | `confirmation of the 10,000₮ share par value` | par role; plain |

Net: **7 `$` tokens removed** from 01. Verifier USD DRIFT 312 → **305 (−7)**; HARD
checks all **0**; VERDICT PASS. (Verifier baseline is re-baselined centrally, not by
me.)

### Judgment note on the DEC-11 rationale prose (items 4–5)
The brief's CONFIRMED table maps `$25/$25.00 in the par role → 10,000₮`. The DEC-11
**Statement** value is unambiguously the par role → changed. The **rationale** column
also stated `$25` twice as the chosen/canonical value; leaving those would make the
normative DEC-11 self-contradictory (statement 10,000₮ vs rationale $25). I updated
both rationale mentions to 10,000₮ and reconciled the "working canonical value"
clause to "provisional working value … pending DEC-11 / legal." This is a **value
change, not a meaning/scope change** — DEC-11 still means "one mandatory,
non-withdrawable, low-barrier par share." "The figure tied to the onboarding flow
spec" stays true because the onboarding-flow figure (F-103, item 2) is changed to
10,000₮ in this same pass. Flagged here for reviewer sanity-check because rationale
prose (not just the bare value) was touched.

## HELD tokens (left as `$…`, deliberate) — [VERIFIED against brief HELD sets]
| line | token | held-set | reason |
|---|---|---|---|
| 19 | `$55k–$90k/year` (P-1) | HELD-2 persona incomes | USD income basis the migration re-anchors AGAINST; routed to income-basis task |
| 47 | `$35k–$60k/year` (P-2) | HELD-2 | " |
| 61 | `$45k–$70k/year` (P-3) | HELD-2 | " |
| 107 | `≤ $25` CPA (KPI-1.4) | HELD-1 KPI cluster | KPI re-baselining is a separate (market-size-driven) task |
| 113 | `$25M` … `$150M` AUM (KPI-2.1) | HELD-1 | magnitude-truncated; true values $25M/$150M; KPI task |
| 114 | `≥ $1,500` avg deposit (KPI-2.2) | HELD-1 | KPI task |
| 135 | `≥ $1.5M` community capital (KPI-4.4) | HELD-1 | KPI task |
| 367 | `$25 (Sprint 2) vs $5 (Sprint 3)` (DEC-11 alternatives-considered column) | HELD-6 superseded/draft record | Historical record of what each sprint proposed. `$5 (Sprint 3)` = rejected par draft (explicit in HELD-6). `$25 (Sprint 2)` = the sprint that ORIGINATED the value; re-denominating it would falsify the deliberation record (Sprint 2 proposed $25, not ₮10,000). Both held as history. |

## UNDETERMINED / OUT-OF-SCOPE — flagged, NOT changed (HONESTY: HOLD, do not guess)
- **DEC-18 currency convention (line 374)** and the **canonical-term row `USD,
  integer minor units` (line 406)** still read **USD / ISO-4217 `USD`**. These use the
  word "USD", are **not `$` amount tokens**, and are **not in the brief's CONFIRMED or
  HELD tables**. Changing DEC-18 from `USD` to `MNT` / ISO-4217 `496` (minor unit 2) is
  a **normative currency-policy decision** for the currency-policy owner, not an
  amount-application edit — and the task's own rule is "STOP and report if a value
  change implies a meaning change" to a DEC-1…20 term. **HELD pending a coordinator/PO
  decision.** Note the residual tension: 01 now states par in ₮ while DEC-18 still
  declares USD as the program currency. This is expected mid-migration and should be
  resolved by whoever owns the DEC-18 code flip across all docs (00–05), consistently.

## Not applicable in 01 (confirmed absent, no action)
Step-up / P2P velocity / loan-min / loan-max / AML-aggregate limit definitions,
above-cap example, round-up amounts, `$88.20` installment, DEC-65/DEC-63 program
caps — **none appear in 01** (they live in 05 and other docs). Nothing to change or
hold here for those.

## Verification
- `.softhouse/verify-docs.sh`: HARD = 0 (all pass); USD DRIFT 312→305 (−7, progress);
  rails 56 (=), vendor 83 (=). VERDICT PASS.
- Header block, DEC/US/EP/E IDs, and table structure preserved; nothing renumbered.

no code executed
