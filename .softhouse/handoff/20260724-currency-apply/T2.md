# T2 — Currency APPLY: 02_user_stories.md (USD → MNT)

Task T2 (apply agent). Scope: edit ONLY `idea-lab/final_requirements/02_user_stories.md`
per `.softhouse/currency-apply-brief.md` (confirmed table) and
`.softhouse/handoff/20260721-currency-policy/T3.md` (worked-example formulas). Values were
**applied as confirmed, not re-derived** — no new anchor math performed here.

11 USD tokens found (`grep -n '\$' idea-lab/final_requirements/02_user_stories.md` before
edit). 6 changed, 5 held. No worked-example arithmetic blocks (§A–M) occur in this document —
all 11 tokens are simple par/fee/held references, not coupled formulas.

## Changed (old → new, role)

| line (pre-edit) | old | new | role |
|---|---|---|---|
| 60 | `$25` | `10,000₮ (provisional, pending DEC-11 / legal)` | Membership share par (DEC-11/OI-1), KEYSTONE. First use in this document — provisional caveat added per brief. "…or the $25 share payment…" |
| 86 | `$25` | `10,000₮` | Membership share par (DEC-11) — EP-2 intro prose, second occurrence, caveat not repeated (already stated at line 60). |
| 90 | `$25.00` | `10,000₮` | Membership share par value, US-2.1 story text ("mandatory membership share (10,000₮ par value)"). |
| 115 | `$25.00` | `10,000₮` | Membership share redemption at par, US-2.4 ("redemption of the share at 10,000₮ par"). |
| 174 | `$0` | `0₮` | Zero-fee (US-4.1 internal P2P transfer, DEC-3). Zero maps to zero per brief. |
| 665 | `$25` (in `(DEC-11 fixes $25)`) | `10,000₮` (in `(DEC-11 fixes 10,000₮)`) | Membership share par — this parenthetical states the CURRENT ratified DEC-11 value (contrasted against the superseded Sprint 3 `$5` draft in the same sentence), so it converts. The `$5` earlier in the same sentence is the superseded draft and was left as-is (see HELD below). |

All six use the whole-tugrik postfix notation (`10,000₮`, `0₮`), no `$`, no `.00`, no float,
per CLAUDE.md and the brief's notation rule.

## HELD (unchanged, with held-set)

| line | token | held-set | reason |
|---|---|---|---|
| 413 | `$1.5M` (KPI-4.4 community capital) | KPI cluster (brief §1) | Explicitly listed HELD in brief: "$1.5M community capital". KPI re-baselining is a separate task. |
| 465 | `$0.00` (whole-dollar round-up transactions) | Round-up amounts (brief §3) | EP-10 round-up mechanic amounts — deferred by PO, no confirmed ₮ denomination. |
| 465 | `$5.00` (batch settle threshold, DEC-67) | Round-up amounts (brief §3) | Same story (US-10.2); explicitly named in brief ("$5.00 batch settle threshold (DEC-67)"). |
| 665 | `$5` (Sprint 3 superseded par draft) | Superseded / de-ratified values (brief §6) | "Sprint 3's $5 share" — historical record of a rejected draft, matches brief's "$10.00/$5 par drafts". Left as-is; only the adjacent current-value parenthetical `(DEC-11 fixes $25)` was converted (see Changed table). |
| 667 | `$1,000` (Sprint 1 microloan cap) | Superseded / de-ratified values (brief §6) | "Sprint 1's $1,000 microloan cap... were draft-level details not ratified" — matches brief's de-ratified-loan-cap held item. Historical record, not carried forward. |

## Undetermined tokens

None. All 11 tokens' roles were determinable from context and the confirmed table / held sets —
no ambiguous polysemous token required a STOP-and-flag.

## Out-of-scope observation (not edited)

Line 11 of this document ("All amounts USD, integer minor units (DEC-18).") is a currency
*convention declaration*, not a dollar-amount token, and does not match the `\$` grep pattern
the brief scopes this run to. DEC-18 itself is normative in `01_business_analysis.md` §6 and is
out of scope for a T2 (02-only) apply pass. Flagging for whoever finalizes the currency-policy
DEC amendment — this line will need updating in lockstep with DEC-18, not silently here.

## Verification

`.softhouse/verify-docs.sh` run after edits:

```
HARD (must be zero): all 5 checks PASS (0)
DRIFT: USD amounts 306 (-6 from 312, baseline) — matches exactly the 6 tokens converted
       US payment rails 56 (= baseline)
       US vendor references 83 (= baseline)
VERDICT: PASS
```

No HARD check regressed. DRIFT dropped by exactly 6, consistent with 6 `$` tokens removed.

## Changes Made

- Edited `idea-lab/final_requirements/02_user_stories.md`: 6 USD→MNT token replacements (lines
  60, 86, 90, 115, 174, 665), all within existing prose — no IDs, headers, or structure changed.
- 5 tokens held per brief's KPI / round-up / superseded sets (lines 413, 465×2, 665, 667).
- Created branch `softhouse/T2-currency-02` from main, committed the doc edit and this handoff.

## Decisions

- Line 665's `$25` inside `(DEC-11 fixes $25)` was treated as a live restatement of the current
  ratified par value (contrast clause against the superseded `$5` draft in the same sentence),
  not itself superseded prose — so it was converted, while the `$5` immediately before it was held.

## Unverified

- None — all applied values were taken directly from the brief's CONFIRMED table; no new
  derivation was performed in this task.

## Blockers

- None for this document. (Share par `P = 10,000₮` remains PROVISIONAL pending DEC-11/legal
  per the brief and T3 — carried forward as a caveat on first use, not a blocker to this apply
  pass.)

## Follow-ups

- When DEC-11 / legal confirms the share par, remove or update the "(provisional, pending
  DEC-11 / legal)" caveat at line 60 across the baseline (tracked centrally, not per-document).
- Line 11's currency declaration ("All amounts USD... DEC-18") should be updated in the same
  pass that amends DEC-18 itself (in `01_business_analysis.md`), not here.

no code executed
