# T6 — Apply USD→MNT to 04_technical_architecture.md

Task T6 (APPLY, currency-apply run). Edited **only**
`idea-lab/final_requirements/04_technical_architecture.md`. Applied the
CONFIRMED table from `.softhouse/currency-apply-brief.md` (values taken as
given, not re-derived) with formulas cross-checked against
`.softhouse/handoff/20260721-currency-policy/T3.md`. No other document
touched. `04`'s header block, `DEC-n`/`US-n.n`/`EP-n`/`E-n` IDs, and
traceability are unchanged.

## Changed tokens (old → new, by role)

| Line(s) | Old | New | Role |
|---|---|---|---|
| 30 | `$25.00` | `10,000₮ (provisional, pending DEC-11 / legal)` | Membership share **par** (Stripe capability row, US-2.1) — first use in doc, so carries the required provisional annotation per brief. |
| 82 | `$25.00` | `10,000₮` | Membership share **par**, onboarding Flow F-A (US-2.1) — provisional tag already established at first use (line 30); not repeated. |
| 84 | `$0` | `0₮` | P2P internal transfer fee, Flow F-B (DEC-3). Zero maps to zero. |
| 104 | `$25.00` (Canonical Resolution column only) | `10,000₮ par (provisional, pending DEC-11 / legal)` | **Par** role in the reconciliation table's *Canonical Resolution* column — the value actually in force today. The Sprint 1/2/3 columns ($10, $25, $5) were left untouched — see HELD below; only the canonical/current cell was re-denominated. |
| 176 | `$25.00` | `10,000₮` | Membership share **par**, E-5 MembershipShare entity narrative (DEC-11). |
| 179 | `` `2500` ($25.00, DEC-11) `` | `` `1000000` (10,000₮, provisional, pending DEC-11 / legal) `` | Schema literal — `par_value` Money field. Re-derived the **minor-unit integer**, not just the display value: 10,000₮ × 100 (ISO minor unit = 2, per CLAUDE.md "store 2") = `1000000`, mirroring how `2500` was `$25.00 × 100`. No float introduced. |
| 811 | `$0 fee` | `0₮ fee` | P2P settlement API response description (`POST /api/v1/payments/p2p`). Zero maps to zero. |

7 tokens changed, matching the brief's PAR and ZERO/FEE roles. `verify-docs.sh`'s USD-amount DRIFT count dropped by exactly 7 (312 → 305), confirming no unintended token was touched and none was missed among the confirmed roles.

## HELD tokens (left as-is, with held-set)

| Line(s) | Token | Held set | Reason |
|---|---|---|---|
| 5 | `$5/$10 share values` | Set 6 — superseded/de-ratified | Header prose listing superseded Sprint-1–3 terms that "do not reappear here except in the reconciliation log." Historical record of rejected par drafts. |
| 86 | `$0.00` (RoundUpConfig, "whole-dollar amounts yield $0.00") | Set 3 — round-up amounts (deferred) | Round-up mechanic value; T3 flags the round-up denomination itself as PENDING (no clean MNT equivalent decided). Cannot be re-expressed without a confirmed denomination. Marked with an inline `<!-- held -->` comment for auditability. |
| 104 | `$10` (Sprint 1 col), `$25` (Sprint 2 col), `$5` (Sprint 3 col) | Set 6 — superseded/de-ratified | Reconciliation table's Sprint 1/2/3 columns document what each **rejected draft** proposed historically. Only the Canonical Resolution cell (see Changed table) reflects the value actually in force, so only it was re-denominated. **Judgment call:** the brief names "$10.00/$5 par drafts" explicitly but not Sprint 2's `$25`; I held it too because the column is describing the same class of historical, superseded per-draft content, not a currently-active figure — flagging this reasoning per the honesty rule rather than silently deciding. |
| 112 | `$1,000 cap hard-coded` (Sprint 1 column) | Set 6 — superseded/de-ratified (de-ratified loan cap, DEC-45 history) | Explicitly named in the brief's held set 6. |
| 328 | `Sprint 1's $1,000/enum-term constants` | Set 6 — superseded/de-ratified | Same de-ratified $1,000 loan-cap history, referenced in the E-19 LoanProduct narrative. |
| 594 | `$5.00` (`batch_threshold` config default) | Set 3 — round-up amounts (deferred, DEC-67 batch settle threshold explicitly named) | Explicitly named in the brief's held set 3 ("$5.00 batch settle threshold (DEC-67)"). Marked with an inline `<!-- held -->` comment for auditability. |

## Undetermined tokens

None. Every `$`-token found by `grep -noE '\$[0-9][0-9,]*(\.[0-9]+)?'` in `04_technical_architecture.md` was classified into either the Changed or HELD table above — no token required a STOP/flag-for-human decision.

## Deliberately NOT touched: "USD" currency-type prose

Line 9 ("All monetary amounts are **USD, integer minor units (cents)** per
DEC-18... implicit currency `USD`..."), line 110 ("Integer minor units,
`USD` (DEC-18)"), and line 750 ("`"currency": "USD"` (DEC-18)") were **left
unchanged**. These are not dollar-amount tokens (no `$`) — they are
declarations of DEC-18 itself (the currency-type decision). DEC-18 is
defined normatively in `01_business_analysis.md` §6, and per CLAUDE.md
("`01` §6 is normative for DEC-1…20; no other document may redefine those
terms or values") `04` cannot unilaterally redefine DEC-18 to MNT. Checked
`01_business_analysis.md:374` — DEC-18 there still reads USD, unchanged by
any currency-apply task so far. Redenominating `04`'s currency-type prose
ahead of `01`'s DEC-18 update would create an internal contradiction, so it
is out of scope for this task and was held pending the DEC-18 redefinition
(a separate, upstream task).

## Verification

Ran `.softhouse/verify-docs.sh` from the worktree root:

```
HARD (must be zero):
  PASS  US deposit-insurance regime (FDIC/NCUA)    0
  PASS  stale MNT 3m RTGS threshold                0
  PASS  first_name / last_name                     0
  PASS  float / double / DECIMAL money types       0
  PASS  hardcoded UTC+8                            0

DRIFT (must not increase):
  PASS  USD amounts       305  (-7 from 312)
  PASS  US payment rails  56   (= baseline)
  PASS  US vendor references 83 (= baseline)

VERDICT: PASS
```

All HARD checks remain at 0 (in particular float-money and hardcoded-UTC).
No new float/decimal money type was introduced — the schema literal at line
179 was re-derived as an integer minor-unit value (`1000000`), consistent
with CLAUDE.md's "store 2" ISO minor-unit rule.

## Sources

- `.softhouse/currency-apply-brief.md` — confirmed table, HELD list, notation rules (read in full).
- `.softhouse/handoff/20260721-currency-policy/T3.md` — derivation formulas and rounding rules (read in full; no value re-derived independently — the brief's confirmed table is authoritative for this run).
- `idea-lab/final_requirements/04_technical_architecture.md` — edited file.
- `CLAUDE.md` — non-negotiables (integer minor units, whole-tugrik postfix notation, DEC normativity).

## Unverified

- The judgment call to hold Sprint 2's `$25` at line 104 alongside the explicitly-named `$10`/`$5` drafts (see HELD table) is my own extension of the brief's stated held set, not a value explicitly listed — flagged above rather than silently applied.

## Blockers

None. All 7 in-scope confirmed-role tokens applied; all held tokens matched or reasonably extended an explicitly named held set; no undetermined token remained.

no code executed
