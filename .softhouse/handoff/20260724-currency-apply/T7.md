# T7 — Apply USD→MNT to `05_prd_and_roadmap.md`

Task T7 (APPLY, doc-only). Edited **only** `idea-lab/final_requirements/05_prd_and_roadmap.md`.
Source of truth: `.softhouse/currency-apply-brief.md` (PO-confirmed table + HELD list) and
`.softhouse/handoff/20260721-currency-policy/T3.md` (formulas). No value re-derived here.

35 USD tokens were in scope (matches brief). **19 CHANGED, 16 HELD, 0 undetermined.**
Verifier `.softhouse/verify-docs.sh`: **PASS** — all HARD = 0; USD DRIFT 312 → **293 (−19)**;
rails/vendor unchanged. No float, no stray `$` or `.00` introduced.

## CHANGED tokens (old → new, by role) — [VERIFIED against brief table]

| Line | old | new | role |
|---|---|---|---|
| 16 | `$25` | `10,000₮` (+ `(provisional, pending DEC-11 / legal)`) | share par — KEYSTONE, first use in doc → provisional note added |
| 51 | `$25.00` | `10,000₮` | share par (US-2.1/DEC-11) |
| 67 | `$0` | `0₮` | P2P fee-zero (DEC-3 internal P2P) |
| 88 | `$0.00` | `0₮` | zero — EP-7 patronage statement |
| 155 (DEC-31) | `$0.00` | `0₮` | zero — goal-target validation bound |
| 157 (DEC-33) | `$2,000.00` ×2 (`/sender`, `/day`) | `1,150,000₮` ×2 | P2P daily velocity limit (0.50·W) |
| 160 (DEC-36) | `$1,000.00` ×2 (`> …`, `> … seed`) | `550,000₮` ×2 | AML external step-up threshold (0.25·W) |
| 169 (DEC-45) | `min $100.00` (desc) | `min 100,000₮` | loan minimum (0.05·W) |
| 169 (DEC-45) | `Min $100.00` (ratified col) | `Min 100,000₮` | loan minimum |
| 169 (DEC-45) | `max $5,000.00` (ratified col) | `max 5,700,000₮` | loan maximum (2.5·W) |
| 169 (DEC-45) | `$5,000` (rationale) | `5,700,000₮` | loan maximum |
| 180 (DEC-56) | `$0.00` | `0₮` | zero — patronage statement |
| 197 (DEC-73) | `$10,000` | `11,400,000₮` | AML aggregate-outbound monitoring (5·W) — **distinct from DEC-65** |
| 303 | `$25.00` | `10,000₮` | share par (S2 exit criteria) |
| 313 | `$0` | `0₮` | P2P fee-zero (S2 exit criteria) |
| 382 (OI-1) | `$25` | `10,000₮` | share par (legal-confirmation open item) |
| 407 | `$5,000` | `5,700,000₮` | loan maximum (adjudicated cap, DEC-45) |

Total distinct changed tokens: **19**.

Notes:
- Share par `10,000₮` applied as **provisional** (KEYSTONE); provisional note placed on
  first doc use (line 16) per brief; subsequent par uses (51, 303, 382) changed without note.
- DEC-45 (line 169) intentionally now mixes denominations: the **confirmed** min/max are ₮,
  while the **de-ratified** `max $1,000.00` and the rationale's `$1,000 cap` stay USD (HELD, below).
  This is per-role correct — the $1,000 is the rejected draft the DEC is amending away from.
- All meanings preserved. No DEC's meaning changed; only monetary values updated. No STOP condition hit.

## HELD tokens (left as USD) — [VERIFIED against brief HELD list]

| Line | token(s) | HELD set | why |
|---|---|---|---|
| 34 | `$1.5M` (matches `$1`) | Set 1 — KPI cluster | KPI-4.4 community capital; sector/market re-baseline is a separate task |
| 109 | `$0.00` | Set 3 — Round-Up (EP-10 deferred) | EP-10 AC theme; round-ups deferred, no confirmed ₮ denomination |
| 169 | `max $1,000.00`; `$1,000 cap` | Set 6 — de-ratified | DEC-45 history: the Sprint-1 de-ratified loan cap being amended away — historical record |
| 187 (DEC-63) | `$1,000`, `$100,000`, `$100k` (→`$100`) | Set 5 — program-config caps | pitch-goal bounds; program-budget decision |
| 189 (DEC-65) | `$500.00`, `$10,000`, `$500`, `$100k`(→`$100`), `$1.5M`(→`$1`) | Set 5 + Set 1 | surplus-match ceiling `$10,000` (NOT the DEC-73 AML $10k) and superseded `$500`; plus `$100k`/`$1.5M` refs |
| 191 (DEC-67) | `$5.00`, `$5.00`, `$5` | Set 3 — Round-Up (deferred) | round-up batch settle threshold; no confirmed ₮ denomination |
| 407 | `$1,000 cap` | Set 6 — de-ratified | de-ratified Sprint-1 loan cap (prose describing rejected decision) |

Total HELD tokens: **16**. 19 changed + 16 held = 35. Reconciles with brief's count.

Critical disambiguations honored:
- DEC-65 `$10,000` surplus-match ceiling **HELD**; DEC-73 `$10,000` AML monitor **CHANGED → 11,400,000₮**.
- DEC-45 confirmed max `$5,000` **CHANGED → 5,700,000₮**; DEC-45 de-ratified `$1,000` cap **HELD**.
- DEC-36 step-up `$1,000` **CHANGED → 550,000₮** (distinct from the de-ratified loan `$1,000`).

## Undetermined tokens
**None.** Every in-scope token resolved to a confirmed role (changed) or a HELD set.

## Verification
- `.softhouse/verify-docs.sh` → VERDICT PASS. HARD checks all 0. USD DRIFT 312→293 (−19). Rails/vendor unchanged.
- Post-edit grep for `$[0-9]` in 05 returns exactly the 16 HELD tokens above; no stray `$` or `.00`
  on any changed line; no float money value introduced.

## Provenance
- Brief: `.softhouse/currency-apply-brief.md` (PO-confirmed anchors; W = median monthly wage ₮2,278,400).
- Derivation formulas: `.softhouse/handoff/20260721-currency-policy/T3.md`.
- Branch `softhouse/T7-currency-05`; only `05_prd_and_roadmap.md` + this handoff committed. main untouched.

no code executed
