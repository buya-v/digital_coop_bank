# T1 handoff — amend DEC-18 normative in 01_business_analysis.md

Branch: `softhouse/T1-dec18-01`
File touched (only): `idea-lab/final_requirements/01_business_analysis.md`
Nature: NORMATIVE amendment (01 §6 owns DEC-1..20). Executes the ratified
Mongolia/MNT direction (CLAUDE.md). Currency TYPE flip USD → MNT (496).
No amount conversions, no vendor edits.

## Edits (3)

### Edit 1 — line ~98, KPI section convention sentence
OLD:
> **All monetary values are USD. All targets are management estimates, not market facts.**
NEW:
> **All amounts MNT (₮, integer minor units, ISO 4217 numeric 496), displayed whole tögrög (DEC-18). A bounded set of values remains in USD pending downstream decisions, each explicitly marked. All targets are management estimates, not market facts.**

Verbatim per brief convention line; the "All targets are management estimates…"
tail was preserved.

### Edit 2 — line ~374, DEC-18 normative row (01 §6)
OLD (statement cell):
> All amounts in this program are **USD**; machine representations use minor units (integer cents) with ISO-4217 code `USD`.
NEW (statement cell):
> All monetary amounts are denominated in Mongolian tögrög (MNT; ISO 4217 alpha `MNT`, numeric `496`; 2 minor units). Stored as integer minor units; displayed as whole tögrög with postfix `₮` and thousands separators (e.g. `1,250,000₮`). Supersedes the original USD denomination (market is Mongolia — see project context). **Transitional exceptions:** a bounded, individually-marked set of values remains in USD pending downstream decisions — KPI/AUM targets (KPI re-baselining), round-up amounts (round-up denomination), the loan-installment example (rate model), and community-funding program caps (program budget).

Verbatim per brief canonical DEC-18 statement. Row structure (Do-NOT-Use +
rationale cells) unchanged. DEC-18 MEANING (currency + amount conventions)
preserved; only currency changed USD → MNT/496.

TRANSITIONAL-EXCEPTIONS CLAUSE PRESENT: yes — enumerates KPI/AUM targets,
round-up amounts, loan-installment example, community-funding program caps as
the bounded USD remainder. [VERIFIED in file]

### Edit 3 — line ~406, canonical-term table row
OLD:
> | `USD`, integer minor units | Money representation (DEC-18). | floats, unstated currency |
NEW:
> | MNT (496), integer minor units | Money representation (DEC-18). | floats, unstated currency |

Per brief. Definition + Do-NOT-Use cells unchanged.

## Not touched (per brief OUT OF SCOPE)
- No already-migrated ₮ amount altered (e.g. par 10,000₮ at 01:386, 01:410 left as-is).
- No HELD USD amount converted (KPIs / round-ups / $88.20 / program caps — the
  new transitional clause covers them explicitly).
- No vendor name touched (Stripe / Persona / Plaid etc.).
- Out-of-scope docs (02, 03, 04, 05, 06, 00) NOT touched — T1 is 01-only.

## Verification
`.softhouse/verify-docs.sh` → VERDICT: PASS (exit 0). [VERIFIED]
- HARD checks all 0 (FDIC/NCUA, stale MNT 3m, first/last name, float money,
  hardcoded UTC+8).
- DRIFT — USD amounts 131 (= baseline, no increase). Unchanged because these
  three were currency-TYPE declarations, not `$`-counted amounts; the brief
  anticipated drift would move little. Type-coherence is the objective, achieved.
- US payment rails 56, US vendor refs 83 — both = baseline, unchanged.

no code executed
