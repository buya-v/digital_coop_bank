# T2 — DEC-18 currency-TYPE flip: 02_user_stories.md and 03_acceptance_criteria.md

Task T2. Scope per `.softhouse/dec18-amendment-brief.md`: flip the currency-type
*declaration* wording (not amounts, not vendors) in two doc-level convention lines
in `02_user_stories.md` and one in `03_acceptance_criteria.md`. Individual USD
amounts were already migrated to ₮ in an earlier run (20260724-currency-apply);
this run only touches the sentences that declare "what currency this document is
denominated in."

## Changed (old → new)

| file | line (pre-edit) | old | new |
|---|---|---|---|
| `02_user_stories.md` | 11 | `All amounts USD, integer minor units (DEC-18).` | `All amounts MNT (₮, integer minor units, ISO 4217 numeric 496), displayed whole tögrög (DEC-18). A bounded set of values remains in USD pending downstream decisions, each explicitly marked.` |
| `02_user_stories.md` | 182 | `FX/multi-currency is out of scope (USD only, DEC-18).` | `FX/multi-currency is out of scope (MNT only, DEC-18).` |
| `03_acceptance_criteria.md` | 473 | `**[CONFIRMED]** USD only, integer minor units (DEC-18); FX out of scope.` | `**[CONFIRMED]** MNT only, integer minor units (DEC-18); FX out of scope.` |

Rationale for the two different treatments:
- Line 02:11 is the full doc-level convention statement the brief lists (alongside
  01:98, 02:182, 03:473, 05:14) for the canonical paragraph replacement — used the
  brief's verbatim canonical wording (brief line 17-18), which explicitly carries
  the "bounded set of values remains in USD" transitional-exceptions clause.
- Line 02:182 and 03:473 are each a short parenthetical/inline "USD only" phrase,
  not the doc-level convention sentence itself. The brief gives an explicit
  minimal-diff instruction for exactly this pattern (brief line 19: `"FX/multi-currency
  out of scope (USD only, DEC-18)" → "(MNT only, DEC-18)"`). Applied the same
  mechanical `USD only` → `MNT only` swap to 03:473's structurally identical
  `USD only, integer minor units (DEC-18); FX out of scope.` clause, preserving the
  rest of the sentence (including the adjacent "; FX out of scope." which was
  already present and unrelated to the type-flip) verbatim.

## Confirm: no amount or vendor touched

- `git diff` for both files shows exactly these 3 line-level edits — no other lines
  changed.
- No numeric literal (par value, fee, limit, KPI, minor-units figure) was altered.
  The `1,150,000₮`, `550,000₮`, `0₮` figures on adjacent lines in 03 (472, 474, 478)
  are untouched.
- No vendor name (`Stripe`, `Plaid`, `FedNow`, `SEPA`, `ACH`) was touched — line
  02:182's "ACH, outbound wire... FedNow / SEPA Instant" rail names are unrelated
  to this DEC-18 currency-type scope and were left exactly as-is.
- HELD USD amounts elsewhere in these two docs (KPIs, round-ups, program caps) were
  not searched for or touched — out of scope for T2, per brief.

## Verification

`.softhouse/verify-docs.sh` run after edits:

```
HARD (must be zero):
  PASS  US deposit-insurance regime (FDIC/NCUA)    0
  PASS  stale MNT 3m RTGS threshold                0
  PASS  first_name / last_name                     0
  PASS  float / double / DECIMAL money types       0
  PASS  hardcoded UTC+8                            0

DRIFT (must not increase — Mongolia migration in progress):
  PASS  USD amounts                                131  (= baseline)
  PASS  US payment rails                           56  (= baseline)
  PASS  US vendor references                       83  (= baseline)

VERDICT: PASS
```

HARD stayed 0. DRIFT did not increase (USD amounts count unchanged at 131 = baseline
— expected, since T2 only touches convention prose, not `$`/amount tokens, and the
02:11 canonical replacement text itself contains the word "USD" once, in the
transitional-exceptions clause, which is prose not a counted amount token).

## Changes Made

- Edited `idea-lab/final_requirements/02_user_stories.md`: 2 convention-line edits
  (lines 11, 182).
- Edited `idea-lab/final_requirements/03_acceptance_criteria.md`: 1 convention-line
  edit (line 473).
- Created branch `softhouse/T2-dec18-0203` from the current worktree HEAD; committed
  the doc edits and this handoff.

## Decisions

- 02:11 got the brief's full canonical paragraph (with the transitional-exceptions
  sentence) because the brief explicitly lists 02:11 among the doc-level convention
  lines slated for that paragraph.
- 02:182 and 03:473 got the minimal `USD only` → `MNT only` mechanical swap, per the
  brief's explicit instruction for that exact phrase pattern, and per this task's
  own line-level guidance. Did not append the full transitional-exceptions clause to
  these two shorter inline phrases, to avoid duplicating/contradicting the "FX...out
  of scope" statement already adjacent in the same sentence.

## Unverified

- None — both source texts (brief + orchestrator task line numbers) matched the
  actual file content exactly at the given line numbers; no interpretation gap.

## Blockers

- None.

## Follow-ups

- 01:98, 01:406(canonical-term row), and 05:14 still carry the pre-flip USD wording
  per the brief's scope list — out of scope for T2, presumably owned by sibling
  tasks in this run.
- 04's schema/API currency-code prose and the two missed par-value minor-units
  literals (04:776, 04:781, 05:1084) are explicitly out of scope for T2 (brief
  scopes those to a currency-code/schema task) — not touched here.

no code executed
