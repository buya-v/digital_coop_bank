# T4 — currency type flip + missed par literals (04_technical_architecture.md)

**Run:** 20260724-dec18-amendment
**Branch:** softhouse/T4-dec18-04
**Scope:** ONLY `idea-lab/final_requirements/04_technical_architecture.md`
**Source of truth:** `.softhouse/dec18-amendment-brief.md` (read in full)

DEC-18 flips the currency **type declaration** USD → MNT. Individual amounts were
already migrated to ₮ in run 20260724-currency-apply. This task only flips currency
**codes** and fixes three minor-units par literals that pass missed (they had no `$`,
so the amount regex skipped them). It does NOT re-migrate amounts and does NOT touch
vendor names.

## Arithmetic (verified)
MNT has 2 minor digits. Par value = 10,000₮. 10,000 × 100 = **1,000,000 minor units**.
So `2500` → `1000000`. Independent cross-check: line 179 already carried
`par_value` Money = `1000000` (10,000₮) from the currency-apply run — the new 776/781
literals now match it exactly.

## Currency-type flips (USD → MNT)
Brief §"04 schema / API surface" says "every `USD` currency code → `MNT`" at
04:9, 110, 209, 221, 750. All done:

| Line | Before | After |
| :--- | :--- | :--- |
| 9 | `All monetary amounts are **USD, integer minor units (cents)**` | `... **MNT, integer minor units (cents)**` |
| 9 | `implicit currency \`USD\`` | `implicit currency \`MNT\`` |
| 9 | `fixed to \`USD\`` | `fixed to \`MNT\`` |
| 110 | `**Integer minor units, \`USD\` (DEC-18).**` | `**Integer minor units, \`MNT\` (DEC-18).**` |
| 209 | `\`amount\` Money (positive); \`currency\` \`USD\`.` | `... \`currency\` \`MNT\`.` |
| 221 | `\`amount\` Money; \`currency\` \`USD\`; \`memo?\`` | `... \`currency\` \`MNT\`; \`memo?\`` |
| 750 | `integer minor units + \`"currency": "USD"\` (DEC-18)` | `... \`"currency": "MNT"\` (DEC-18)` |

**Note on line 9 (all three USD codes flipped, not just two):** the task prompt
enumerated two tokens at line 9 (`implicit currency` and `fixed to`); the brief's
general rule is "every `USD` currency code → `MNT`". Line 9 opens with a third USD
code — "All monetary amounts are **USD**…" — a DEC-18 *reference* statement (04 references
DEC-18, 01 defines it). Leaving it USD would self-contradict the two flipped codes on the
same line and the DEC-18 MNT amendment in 01. Flipped it for type-coherence per the
"every USD currency code" rule.

**Left as-is on line 9 (flagged):** the parenthetical gloss `(cents)` is a USD
minor-unit *term*, not a currency *code*, so it is outside "every USD currency code → MNT"
and outside this task's enumerated scope. It remains `(cents)`, consistent with the other
out-of-scope `cents` occurrences still in the doc (line 657 config keys
`share.par_value_cents` / `roundup.batch_threshold_cents`; line 1084 "2500 cents" in the
Stripe flow). A later minor-unit-terminology cleanup should reconcile these.

## Schema / API JSON currency code (USD → MNT)
- 776 `currency:"USD"` → `currency:"MNT"`. (No `currency:"USD"` exists on 781 — only 776
  carries it; the task's "~781" pointer for currency had nothing to change there.)

## Corrected currency-apply MISS — par literals `2500` → `1000000` (flagged)
These three had no `$`, so the amount-migration regex skipped them. Corrected here:
- 776 `amount:2500` → `amount:1000000`
- 776 `par_value:2500` → `par_value:1000000`
- 781 `redemption:{amount:2500, terms_ref}` → `redemption:{amount:1000000, terms_ref}`

All are integer minor units (no float/decimal). 1,000,000 = 10,000₮, matching the
configured par at line 179.

## Out of scope — confirmed UNTOUCHED
- **Stripe / vendor names:** `payment_token (Stripe)` (776) and all other `Stripe`
  references (lines 30, 44, 72, 82, 224, 977, 1082, 1084) left intact —
  Stripe count 10 before and after. Separate vendor-removal run.
- **04:1084** "configured par value (2500 cents, DEC-11)" + "debit Stripe clearing":
  inside §4.4 Stripe flow → NOT touched here. (Brief assigns the "2500 cents → 1,000,000
  minor units" change to **05:1084** in a different task; 04:1084's cents/Stripe fall in
  the vendor-removal scope.)
- **Held USD amounts / already-migrated ₮ amounts:** not converted.
- **Config `_cents` keys (line 657):** naming, not amounts — untouched.

## Verification
`.softhouse/verify-docs.sh` → **VERDICT: PASS**.
- HARD all 0 (incl. "float / double / DECIMAL money types" = 0).
- DRIFT: USD amounts 131 (= baseline, did not increase — did not drop, because these edits
  are currency *codes* and non-`$` literals, which the `$`-counted drift metric does not
  track; brief anticipated this — "the point is type-coherence, not the drift number").
- Post-edit `grep USD` on 04 = 0 occurrences.

no code executed
