# T5 — DEC-18 amendment: currency declaration + missed par literal in `05_prd_and_roadmap.md`

Task T5 (APPLY, edit-only, doc-scoped). Scope per the task brief: edit **only**
`idea-lab/final_requirements/05_prd_and_roadmap.md`, per
`.softhouse/dec18-amendment-brief.md`. Individual amounts were already migrated
in run `20260724-currency-apply` (see `T7.md` in that handoff dir, which covers
this same file); this run flips the currency **type declaration** and was
supposed to fix a missed minor-units par literal.

## Edit 1 — currency declaration (line 14) — DONE

| | |
|---|---|
| File | `idea-lab/final_requirements/05_prd_and_roadmap.md` |
| Line | 14 |
| Old | `delivered inside regulated, USD, fiat banking.` |
| New | `delivered inside regulated, MNT, fiat banking.` |

Matches the brief's instruction verbatim (`05:14 "regulated, USD, fiat banking" →
"regulated, MNT, fiat banking"`). This is the doc-convention currency-type flip;
it does not touch any amount, and it does not affect the verifier's `n_usd`
DRIFT count (that regex matches `$`-prefixed amounts, not the word "USD").

## Edit 2 — "missed par literal" — NOT DONE, brief/task location error

The task instructed: line ~1084 of `05_prd_and_roadmap.md`, text
`"configured par value (2500 cents, DEC-11)"` → `"configured par value
(1,000,000 minor units = 10,000₮, DEC-11)"`.

**This text does not exist in `05_prd_and_roadmap.md`.** The file is 456 lines
long (confirmed: `wc -l`), so line 1084 is past end-of-file, and a full-text
grep for `2500`, `cents`, and `configured par value` in this document returns
no matches.

The text is real, but it lives in **`04_technical_architecture.md` line 1084**,
not `05`:

> `04:1084` — "**Flow:** onboarding client tokenizes card/wallet via **Stripe
> Elements** → `POST /onboarding/share-purchase` creates a payment intent for
> exactly the configured par value (2500 cents, DEC-11) with `member_ref`
> metadata → on `payment_intent.succeeded` (webhook) the ledger posts
> `SHARE_SUBSCRIPTION` (credit Membership Share equity account, debit **Stripe**
> clearing) …"

Cross-checking the amendment brief itself (`.softhouse/dec18-amendment-brief.md`)
confirms this is a mislabel, not a second, distinct instance: the "05:1084" bullet
sits inside the brief's `## 04 schema / API surface` section, immediately after
two other `04:` line references (`04:776`, `04:781`) for the *same* par-literal
family (`amount:2500` / `par_value:2500` / `redemption:{amount:2500}`), all part
of the one `POST /onboarding/share-purchase` flow. "05:1084" is almost certainly
a typo for "04:1084" that then propagated verbatim into this task's brief.

Two independent reasons I did not act on it:
1. **Out of my scope.** T5's instructions restrict edits to `05_prd_and_roadmap.md`
   only. The actual literal is in `04_technical_architecture.md`, a different file.
2. **Entangled with vendor text that is explicitly out of scope for this run
   regardless of file.** The same sentence at `04:1084` contains "Stripe
   Elements" and "debit Stripe clearing" — both named verbatim in this task's
   "DO NOT touch" list (vendor removal is a later run) and in the brief's
   OUT-OF-SCOPE section (`.softhouse/dec18-amendment-brief.md` §"OUT OF SCOPE
   for this run"). Editing the par-value number in that sentence without
   touching the vendor names is doable, but the location mismatch alone was
   enough to stop rather than guess which file/line the task author meant.

**No edit was made to `04_technical_architecture.md`.** I did not touch it.

Recommendation: route the `04:776` / `04:781` / `04:1084` minor-units par-literal
fixes (all three already scoped correctly in the brief as `04:` lines) to a task
explicitly scoped to `04_technical_architecture.md`, decoupled from any Stripe
vendor-name edit in the same sentence.

Verification math for the record (not applied anywhere by this task, since the
target text was unreachable in scope): 10,000₮ × 100 minor units/₮ (MNT has 2
minor digits per DEC-18) = 1,000,000 minor units. Confirmed correct, matches the
brief's stated value.

## Vendor names — confirmed untouched

`grep -c -i stripe idea-lab/final_requirements/05_prd_and_roadmap.md` → **6**,
same as before this run (lines 51, 54, 226, 392, 393, and one more). No Stripe /
Stripe Elements / "debit Stripe clearing" token was edited. No held USD amount
(KPI, round-up, program-cap) was converted. No already-migrated ₮ amount was
touched.

## Verification

`.softhouse/verify-docs.sh`:

```
HARD (must be zero):
  PASS  US deposit-insurance regime (FDIC/NCUA)    0
  PASS  stale MNT 3m RTGS threshold                0
  PASS  first_name / last_name                     0
  PASS  float / double / DECIMAL money types       0
  PASS  hardcoded UTC+8                            0

DRIFT (must not increase — Mongolia migration in progress):
  PASS  USD amounts                                131  (= baseline)
  PASS  US payment rails                           56  (= baseline)
  PASS  US vendor references                       83  (= baseline)

VERDICT: PASS
```

HARD stayed 0. DRIFT counters unchanged (expected — this run touched a type
word, not a `$`-amount, and made no vendor/rails edit).

## Summary of edits made

| # | File | Line | Old → New |
|---|---|---|---|
| 1 | `idea-lab/final_requirements/05_prd_and_roadmap.md` | 14 | `regulated, USD, fiat banking` → `regulated, MNT, fiat banking` |

Edit 2 from the task brief (par-literal at "05:1084") was **not applied** — the
target text does not exist in this file; see explanation above. Flagged as a
brief/task location error (should read `04:1084`), not silently acted on and not
guessed at.

## Provenance

- Brief: `.softhouse/dec18-amendment-brief.md`.
- Branch: `softhouse/T5-dec18-05`.
- Only `05_prd_and_roadmap.md` and this handoff were committed.

no code executed
