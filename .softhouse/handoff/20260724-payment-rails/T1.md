# T1 — Payment rails in `04_technical_architecture.md` (canonical source)

**Branch:** `softhouse/T1-rails-04`
**Scope:** ONLY `idea-lab/final_requirements/04_technical_architecture.md`. No code, build, deploy, or tests run.
**Gate:** `.softhouse/verify-docs.sh` — HARD stays **0**; rails DRIFT **56 → 43 (-13)**; vendor **83 = baseline** (unchanged); USD **131 = baseline** (unchanged).

Marking: [VERIFIED] = confirmed by grep/gate output in this run. [UNVERIFIED] = judgement/wording call for PO confirmation.

## Canonical `rail` ENUM change [VERIFIED]
`INTERNAL_P2P | ACH | WIRE | RTP` → **`INTERNAL_P2P | ACH_PLUS | RTGS`**.
- `ACH → ACH_PLUS`; `WIRE → RTGS`; **`RTP` folds into `ACH_PLUS`** (ACH+ is 24/7, so there is no separate real-time rail). Enum drops 4 → 3 members.
- Applied at both canonical sites: entity E-16/PaymentSchedule field (line ~290) and the `POST /api/v1/payments/external` request contract (line ~814).

## Every rail term changed (old → new + context)

| Line | Old | New | Context |
|---|---|---|---|
| 29 | `ACH origination/receipt, wire, real-time rails (FedNow), settlement of card network activity` + `<!-- TODO(rails) ... -->` | `ACH+ origination/receipt (24/7), Banksüljee (RTGS) large-value settlement, NETC card-network settlement` | Sponsor-bank/BaaS provider rail column. **TODO(rails) comment resolved and REMOVED** — applied exactly what it described (RTGS/Banksüljee, ACH+, NETC). [VERIFIED comment gone] |
| 46 | `ACH/wire/FedNow via sponsor bank` | `external transfers over Mongolia rails — ACH+ (24/7) and Banksüljee (RTGS) for large-value — via sponsor bank` | S-4 Payments Service description. |
| 219 | `type` enum: `... EXTERNAL_ACH_IN \| EXTERNAL_ACH_OUT \| WIRE_OUT \| RTP_IN \| RTP_OUT \| CARD_PURCHASE ...` | `... EXTERNAL_ACH_PLUS_IN \| EXTERNAL_ACH_PLUS_OUT \| RTGS_OUT \| CARD_PURCHASE ...` | Transaction `type` enum. **Consequential change beyond the two explicitly-numbered enum lines** — done for internal consistency: a `WIRE_OUT`/`RTP_*` transaction type is contradictory once the rail enum drops WIRE/RTP. Mapping: `EXTERNAL_ACH_IN` + `RTP_IN` → `EXTERNAL_ACH_PLUS_IN`; `EXTERNAL_ACH_OUT` + `RTP_OUT` → `EXTERNAL_ACH_PLUS_OUT`; `WIRE_OUT` → `RTGS_OUT`. Mirrors the original (no `WIRE_IN` existed, so no `RTGS_IN` added). **[UNVERIFIED] — PO/reviewer please confirm the fold-in of RTP_IN/RTP_OUT into ACH_PLUS variants and the drop from 6 → 3 external type members.** |
| 290 | `rail` Enum `INTERNAL_P2P \| ACH \| WIRE \| RTP` | `INTERNAL_P2P \| ACH_PLUS \| RTGS` + routing note (threshold ₮5,000,000, Governor's order, US-12.5; RTP folds into ACH_PLUS) | Canonical rail enum, site 1. |
| 814 | `rail` (`ACH\|WIRE\|RTP`) | `rail` (`ACH_PLUS\|RTGS`; omit to auto-route by `amount` against the configured threshold) | Canonical rail enum, site 2 (API request contract). |
| 980 | `ACH/wire/RTP status, returns (with codes)` | `ACH+/Banksüljee (RTGS) status, returns (with the settlement operator's return codes — config, TBD ...)` | BaaS webhook description (prose rail list). |
| 1078 | `ACH origination/receipt, outbound wire, FedNow as available, settlement of Lithic network activity` | `ACH+ origination/receipt (24/7, covering the real-time retail case), outbound Banksüljee (RTGS) for large-value transfers, settlement of card-network activity via NETC (Lithic-issued cards)` | §4.3 BaaS Scope. Card-network concept → **NETC**; **Lithic vendor name retained** (issuer of the cards). |
| 1079 | `Flow (outbound ACH example)` | `Flow (outbound ACH+ example)` | §4.3 flow header. |
| 1080 | `card-network settlement` (reconciliation); `rail unavailable (e.g., FedNow) ⇒ offer ACH fallback` | `NETC card-network settlement`; rewritten fallback logic (below-threshold stays on ACH+ (24/7); above-threshold requires Banksüljee (RTGS), queued to next settlement window; never a silent downgrade) | §4.3 Failure handling. |

### Routing rule inserted
Stated at the enum (line 290) and the API contract (line 814): external transfers route by amount against a **configurable threshold (₮5,000,000, Governor's order, US-12.5)** — `> threshold → RTGS (Banksüljee)`; `≤ threshold → ACH+ (24/7)`. Framed as **configuration**, never hard-coded. **"3m" / "₮3,000,000" never written** (HARD 3m-threshold check = 0). [VERIFIED]

## Return codes — HONESTY (invented nothing) [VERIFIED]
No `R01` literal exists in doc 04 (the only `R01` in the baseline is in `03_acceptance_criteria.md:538`, which is OUT OF SCOPE for T1). Places where a US return-code literal/US-specific phrasing was replaced with the human reason + config/TBD note (no Mongolian code fabricated):

1. **Line 220** (`Transaction.status` / `failure_reason?`): `(e.g., ACH return code)` → `(human-readable return reason, e.g. an insufficient-funds return; the specific return-code catalogue is the settlement operator's, carried as configuration — TBD pending Bank-of-Mongolia settlement-agent selection)`.
2. **Line 980** (BaaS webhook): `returns (with codes)` → `returns (with the settlement operator's return codes — config, TBD pending settlement-agent selection)`.
3. **Line 1079** (§4.3 flow): `the return code in failure_reason` → `the settlement operator's return code (config, TBD pending settlement-agent selection) in failure_reason`.

## Vendors — confirmed UNTOUCHED [VERIFIED]
Stripe / Plaid / Lithic left in place (their remap is the next run). Gate vendor count unchanged at **83 = baseline**. On line 1078 the card-network *concept* was mapped to NETC while the **Lithic** vendor name was deliberately retained as the card issuer. No amounts changed (USD gate 131 = baseline); the only monetary figure added is the ₮5,000,000 threshold, framed as config.

## One residual noted (RTP reference, intentional)
Line 290's explanatory note keeps the word `RTP` once — `the former RTP real-time rail folds into ACH_PLUS` — to document the migration rationale as the brief's enum section requires. Gate-safe (rails DRIFT counts only ACH/FedNow/SEPA, not RTP).

## Out of scope (left as-is, per brief)
- Docs `00`, `03`, `05`, `06` — not edited (R01 in `03` belongs to a different task/run).
- Vendor-name remap (Stripe/Plaid/Lithic) — next run.
