# T3 handoff — payment rails migration in 01_business_analysis.md

Branch: `softhouse/T3-rails-01`
File touched (only): `idea-lab/final_requirements/01_business_analysis.md`
Nature: terminology migration (US rails → Mongolia rails), per
`.softhouse/payment-rails-brief.md`. No vendor edits, no amount changes.

## Rail terms changed (old → new, with context)

### Edit 1 — line ~177, CAP-4.2 (capability tree, CAP-4 Payments & Transfers)
OLD:
> `│   ├── CAP-4.2  External Payments (ACH, wire; real-time rails: FedNow / SEPA Instant as available)`

NEW:
> `│   ├── CAP-4.2  External Payments (ACH+ ≤ threshold, 24/7; Banksüljee (RTGS) > threshold)`

Terms removed: `ACH`, `wire`, `FedNow`, `SEPA Instant`. Per the brief's canonical
mapping — ACH (low-value/batch) → **ACH+**; wire (large-value) → **Banksüljee
(RTGS)**; FedNow/RTP/"real-time rail" → folds into ACH+ (24/7, no separate
real-time rail); SEPA → removed (not applicable, no cross-border rail in
scope). Threshold left unstated as a number (consistent with "treat as
configuration, never hard-coded" — CLAUDE.md / brief); the routing rule and the
actual ₮5,000,000 figure live in `02_user_stories.md` US-4.2 / US-12.5 (not
touched by this task — out of scope, file 02 belongs to a different task in
this run).

### Edit 2 — line ~274, F-113 (feature catalog, Payments & Cards)
OLD:
> `| F-113 | External Payments (ACH / Wire / Real-Time Rails) | CAP-4.2 | P-3, P-4 | Inbound/outbound external transfers with same-day and real-time options where rails permit. | MVP |`

NEW:
> `| F-113 | External Payments (ACH+ / Banksüljee RTGS) | CAP-4.2 | P-3, P-4 | Inbound/outbound external transfers routed by amount against a configurable threshold: ACH+ (24/7) at or below threshold, Banksüljee (RTGS) above threshold. | MVP |`

Terms removed from title: `ACH`, `Wire`, `Real-Time Rails`. Terms removed from
description: `same-day and real-time options where rails permit` (vague,
US-rail-shaped phrasing) → replaced with the brief's routing rule stated in
prose (amount-threshold routing, ACH+ 24/7 vs Banksüljee RTGS), matching the
canonical routing-rule language in the brief without hard-coding the
₮5,000,000 figure in this capability-summary table (kept consistent with Edit
1's treatment).

## Deliberately NOT touched

- **`01:362` DEC-6 (Member name & address model)** — left exactly as-is per
  task instruction. The sentence `The original rationale's "ACH/wire formats"
  clause is **retired as market-invalid** — that rail does not exist in
  Mongolia — not as reasoning-invalid; the remaining rationale is unaffected.`
  is a *historical retirement note* explaining why a prior justification for
  the three-part name model was dropped (it referenced US ACH/wire file
  formats, which don't exist here). It is not an active rail reference —
  migrating it (e.g. rewriting "ACH/wire formats" to "ACH+/Banksüljee
  formats") would be wrong: it would assert Mongolia rails require a specific
  name-format accommodation, which is not what the sentence says or means.
  The instruction was explicit on this and it was followed literally.
- No other CAP-4 rail mentions exist in `01_business_analysis.md` — confirmed
  by grep for `CAP-4`, `External Payment`, and `F-113` across the whole file;
  only the tree entry (177) and the feature-table row (274) reference rails.
- No vendor names touched (Stripe/Plaid/Lithic/Persona) — none present in this
  file's rail context; out of scope per brief regardless.
- No amounts changed or introduced — no `₮5,000,000` literal was added; no
  "3m"/"3,000,000"/"3 million" pattern introduced anywhere.
- Only `01_business_analysis.md` was edited. `02`, `03`, `04`, `05`, `06`, `00`
  not touched (other tasks' scope / explicitly out of scope per brief).

## Verification

`.softhouse/verify-docs.sh` → VERDICT: PASS (exit 0). [VERIFIED]

```
HARD (must be zero):
  PASS  US deposit-insurance regime (FDIC/NCUA)    0
  PASS  stale MNT 3m RTGS threshold                0
  PASS  first_name / last_name                     0
  PASS  float / double / DECIMAL money types       0
  PASS  hardcoded UTC+8                            0

DRIFT (must not increase — Mongolia migration in progress):
  PASS  USD amounts                                131  (= baseline)
  PASS  US payment rails                           52  (-4 from 56)
  PASS  US vendor references                       83  (= baseline)
```

- HARD stayed 0, as required.
- Rails DRIFT dropped 56 → 52 (-4): removed `ACH` (×2, tree + feature title),
  `FedNow`, `SEPA` from the counted set. (`ACH+`, `wire`/`Wire`, and
  `Banksüljee`/`RTGS` are not counted by the gate's `n_rails` pattern, per
  `verify-docs.sh`'s own comment — it only tracks `ACH\+|\b(ACH|FedNow|SEPA)\b`
  excluding literal `ACH+`.)
- USD and vendor DRIFT unchanged (= baseline) — expected, this task touched
  neither.

no code executed
