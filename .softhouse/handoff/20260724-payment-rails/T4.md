# T4 — payment rails in 02_user_stories.md

Scope: `idea-lab/final_requirements/02_user_stories.md` only, US-4.2 (External
Payments). Source of truth: `.softhouse/payment-rails-brief.md`.

## Rail terms changed (old → new + context)

1. **Heading, line ~179** — story title
   - Old: `### US-4.2 — External Payments (ACH, Wire, Real-Time Rails)`
   - New: `### US-4.2 — External Payments (ACH+, Banksüljee RTGS)`

2. **Description, line ~182** — rail list + routing rule
   - Old: `Inbound and outbound ACH, outbound wire, and real-time rails (FedNow / SEPA Instant as available); external account linking with verification; cut-off times, status tracking, returns/failure handling, and step-up authentication above thresholds (US-1.4). Fees and limits configured via US-12.5; all flows monitored by US-13.1. FX/multi-currency is out of scope (MNT only, DEC-18).`
   - New: `External transfers route by amount against a configurable threshold (₮5,000,000, set by Governor's order): above the threshold via Banksüljee (RTGS); at or below it via ACH+ (24/7, also covering the instant/real-time retail case — there is no separate real-time rail). External account linking with verification; cut-off times (RTGS only — ACH+ is 24/7), status tracking, returns/failure handling, and step-up authentication above thresholds (US-1.4). Fees and limits configured via US-12.5; all flows monitored by US-13.1. FX/multi-currency is out of scope (MNT only, DEC-18).`
   - Applied the brief's routing rule verbatim (threshold as configuration per
     US-12.5, never hard-coded value baked into prose beyond the ₮5,000,000
     figure itself, which the brief explicitly allows/requires as the
     authoritative Mongolian-page figure). Kept the trailing FX/multi-currency
     sentence unchanged as instructed. Added a short clause noting ACH+ is
     24/7 vs. RTGS having cut-off times, since the original mentioned
     "cut-off times" generically and that detail only applies to the RTGS
     leg now that ACH+ is always-on — this is a clarification, not a scope
     change.

3. **INVEST note, line ~186**
   - Old: `Multi-rail integration; if it exceeds a sprint, split by rail (ACH first, then wire, then real-time) — each rail is independently shippable.`
   - New: `Multi-rail integration; if it exceeds a sprint, split by rail (ACH+ first, then Banksüljee RTGS) — each rail is independently shippable.`
   - Collapsed from three phases to two since ACH+ absorbs the former
     "real-time rail" phase per the brief's canonical mapping (no separate
     FedNow/RTP equivalent exists in Mongolia).

## Amounts

No amounts changed or re-derived. US-4.2 in `02_user_stories.md` contains no
scenario dollar/tögrög figures requiring rail-driven re-derivation (that
pattern applies to `03`, per the brief, which is out of scope for this task).
No "3m" threshold value was introduced anywhere; only "₮5,000,000" is used,
matching the brief's authoritative-Mongolian-page figure.

## TBD/config markers

None needed for this file — US-4.2 does not reference a return-code
catalogue (NACHA R01 etc.) or any other item requiring a TBD/config note.
The threshold itself is already framed as "configurable" / "set by
Governor's order," consistent with the brief's config-not-hardcode rule.

## Out of scope, left untouched

- No vendor-name edits (Stripe/Plaid/Lithic/Persona) — none present in this
  section.
- No changes outside `idea-lab/final_requirements/02_user_stories.md`.
- No changes to `00_*` or `06_ledger_addendum.md` (not touched, per brief).
- Grepped the whole file for `ACH`, `wire`, `FedNow`, `SEPA`, `RTP`,
  `real-time rail`, `Banksüljee`, `RTGS`, `NETC` — the only genuine hits are
  the three items above (a `sepa` grep hit at line ~500 was a false positive
  inside the word "separation," not a rail term; left untouched).

## Verification

Ran `.softhouse/verify-docs.sh`:
- HARD: all 5 checks PASS, total 0 (incl. "stale MNT 3m RTGS threshold" = 0).
- DRIFT — US payment rails: 51, down 5 from the 56 baseline (expected
  direction: dropping toward zero as Mongolia terms replace US terms).
- DRIFT — USD amounts and US vendor references: unchanged at baseline (131,
  83), as expected since this task did not touch currency or vendor terms.
- VERDICT: PASS.

No code executed.
