# T5 — payment rails in `05_prd_and_roadmap.md`

Branch: `softhouse/T5-rails-05`. Scope: US → Mongolia rail-term migration only, per
`.softhouse/payment-rails-brief.md`. No code executed; no build/deploy/tests
(requirements-document edit only, per CLAUDE.md's "no test suite / no build"
verification note).

## Rail terms changed (old → new + context)

1. **EP-4 requirement** (`05_prd_and_roadmap.md:65`)
   Old: `external ACH/wire/real-time-rail payments with account linking, cut-offs,
   returns handling, and step-up above thresholds (US-4.2)`
   New: `external ACH+ / Banksüljee (RTGS) payments — routed by the configurable
   ₮5,000,000 threshold (Governor's order): > threshold → Banksüljee (RTGS),
   ≤ threshold → ACH+ (24/7) — with account linking, cut-offs, returns handling,
   and step-up above thresholds (US-4.2)`
   Added the routing rule inline (per brief's "Routing rule (state it wherever
   rail choice is described)") since the sentence already promised "step-up above
   thresholds" — the threshold is now named and marked as Governor's-order
   configuration, not hard-coded.

2. **US-4.2 story-table row** (`05_prd_and_roadmap.md:236`)
   Old: `External payments (ACH/wire/RTP)`
   New: `External payments (ACH+/RTGS)`
   Kept the existing "Rail-by-rail split available" note in the Notes column
   unchanged, as instructed.

3. **S2 exit criteria** (`05_prd_and_roadmap.md:313`)
   Old: `first ACH round-trip (including a return) processed against the
   sponsor-bank sandbox`
   New: `first ACH+ round-trip (including a return) processed against the
   sponsor-bank sandbox`
   "sponsor-bank sandbox" left as-is — it names the BaaS procurement dependency
   (R-2), not a rail, and is out of scope for this pass.

4. **R-2 risk row** (`05_prd_and_roadmap.md:392`)
   Old: `the entire money-rail stack (FBO structure, ACH/wire/FedNow, card
   settlement)`
   New: `the entire money-rail stack (FBO structure, ACH+/Banksüljee RTGS, NETC
   card settlement)`
   Card settlement now names NETC (Bank of Mongolia entity) per the brief's
   cards note — terminology only; EP-5 (Cards) remains entity-gated /
   not-buildable and this change does not imply otherwise.

## Amounts

No monetary amounts changed. No rail-driven re-derivations were needed in this
file (unlike `03`, this document doesn't stage a sub-threshold "wire" example
that needed to be pushed above ₮5,000,000 to keep the routing distinction
coherent). Never wrote "3m" anywhere — `verify-docs.sh`'s stale-3m-threshold
check stayed at 0.

## Vendor names — confirmed untouched (next run's scope)

`Stripe` appears at lines 51, 54, 226, 303, 392 (R-2 prose) and `Persona`,
`Plaid`, `Lithic`, `Jumio` appear in R-3 (line 393) and elsewhere. All left
exactly as found — grepped post-edit to confirm no vendor token was touched.
Per the brief, vendor migration is explicitly the next run.

## TBD / config — nothing invented

No return-code catalogue, NACHA-style code, or Mongolian-specific code scheme
appears anywhere in `05` to begin with, so nothing needed to be marked TBD in
this file (that concern is `04`'s, out of scope here). The ₮5,000,000 threshold
is stated as Governor's-order configuration in the new EP-4 sentence, consistent
with US-12.5 and the brief's routing-rule wording — not hard-coded as a literal
elsewhere in the doc.

## Out of scope, confirmed left alone

- `06_ledger_addendum.md` and `00_*` — not touched (controller rewrite / separate
  Mongolia rewrite pending, per brief).
- No other file in `idea-lab/final_requirements/` was edited — only
  `05_prd_and_roadmap.md`.

## Verification

`.softhouse/verify-docs.sh` run after the edits:

```
HARD (must be zero): all 5 checks PASS at 0 (stale MNT 3m RTGS threshold: 0).
DRIFT: US payment rails dropped 56 → 51 (-5, all from this file's 4 edit sites).
       USD amounts and US vendor references unchanged (= baseline), as expected
       since this run touched neither.
VERDICT: PASS
```

No code executed.
