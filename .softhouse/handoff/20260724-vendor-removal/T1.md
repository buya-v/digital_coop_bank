# T1 — Vendor removal in `04_technical_architecture.md`

**Task:** Remove US vendor names (Stripe / Plaid / Lithic / Persona / Jumio) from the canonical
technical architecture doc, rename the vendor-leaked schema identifiers/error codes/webhook paths
to role-neutral names per the canonical rename table, and neutralize vendor prose to role
abstractions — without inventing a Mongolian vendor.

**Scope:** `idea-lab/final_requirements/04_technical_architecture.md` ONLY. No other doc, no code.

**Result:** `.softhouse/verify-docs.sh` → **VERDICT: PASS**. HARD checks all 0.
Vendor DRIFT 83 → 29 (−54). 04's own vendor-reference count is now **0** (all 29 remaining live
in other docs — 01/02/03/05, out of T1 scope). USD amounts (131) and US-rails (20) counts
**unchanged** (= baseline) — no rail term or amount was touched.

---

## 1. Identifiers renamed (old → new, with location)

| Old (vendor-named) | New (neutral) | Location(s) in 04 |
|---|---|---|
| `persona_inquiry_id` | `kyc_inquiry_id` | E-2 field (§2.2), API response (§3.1 kyc/sessions), §4.1 flow step 1 |
| `plaid_item_ref` | `account_link_ref` | E-14 field (§2.4) |
| `plaid_public_token` | `account_link_public_token` | §3.4 `POST /external-accounts` request body |
| enum `PLAID_INSTANT` (verification_method) | `INSTANT_LINK` | E-14 `verification_method` enum (§2.4) |
| error `502 PLAID_UNAVAILABLE` | `502 ACCOUNT_LINK_PROVIDER_UNAVAILABLE` | §3.4 `POST /external-accounts` error cases |
| webhook `/webhooks/persona` | `/webhooks/kyc` | §3.14 webhooks table; §3.15 US-1.2 traceability |
| webhook `/webhooks/stripe` | `/webhooks/payments` | §3.14 webhooks table; §3.15 US-2.1 traceability |
| webhook `/webhooks/lithic/authorization` | `/webhooks/card/authorization` | §3.14 webhooks table; §3.15 US-5.3 traceability |
| webhook `/webhooks/lithic/events` | `/webhooks/card/events` | §3.14 webhooks table |
| webhook `/webhooks/plaid` | `/webhooks/account-link` | §3.14 webhooks table; §3.15 US-4.2 traceability |

**Deliberately kept (already neutral, per brief):** `processor_token_ref` (E-14), `issuer_card_ref` (E-18).
`502 VENDOR_UNAVAILABLE` (§3.1 kyc/sessions, §4.1) kept — it is already generic (no vendor name).

## 2. Prose / diagram vendor references neutralized (old → new)

Role abstractions used: **eKYC / identity-verification provider · account-linking provider ·
payment processor · card issuer-processor.**

- **§1.1 actor table** — `Persona` row → **eKYC / identity-verification provider** (added: provider = procurement TBD; ХУР/XYP compliant-alternative candidate, `01` §6.3 BQ2). `Plaid` row → **Account-linking provider** (TBD). `Stripe` row → **Payment processor** (TBD). `Lithic (card issuer-processor)` row → **Card issuer-processor** (TBD; EP-5 entity-gated note).
- **§1.2 service table** — S-1 "Persona integration" → "eKYC-provider integration"; S-2 "Stripe integration" → "payment-processor integration"; S-4 "Plaid linking" → "external account linking via the account-linking provider"; S-5 "Lithic integration" → "Card issuer-processor integration".
- **§1.2 mermaid nodes** — `PERSONA[Persona eKYC]` → `KYC[eKYC provider]`; `PLAID[Plaid]` → `LINK[Account-linking provider]`; `STRIPE[Stripe]` → `PAY[Payment processor]`; `LITHIC[Lithic]` → `CARDPROC[Card issuer-processor]`.
- **§1.3 F-A** — "runs through a Persona inquiry … Persona's webhook" → "runs through a KYC inquiry with the eKYC provider … the eKYC provider's webhook"; "collected by Stripe (card/wallet)" → "collected by the payment processor (card/wallet)".
- **§1.3 F-C** — "Lithic forwards each authorization" → "The card issuer-processor forwards each authorization".
- **§1.3 F-D** — "open-banking data (Plaid)" → "open-banking data (via the account-linking provider)".
- **§2.2 E-2** — "One Persona verification attempt" → "One eKYC-provider verification attempt"; field comment "vendor reference (DEC-5)" → "eKYC-provider inquiry reference (DEC-5)"; "vendor OCR output" → "eKYC-provider OCR output".
- **§2.3 E-8** — `external_ref` comment "(sponsor bank / Stripe / Lithic ID)" → "(sponsor bank / payment processor / card issuer-processor ID)".
- **§2.4 E-14** — desc "linked via Plaid" → "linked via the account-linking provider"; field comment "vendor references" → "account-linking-provider references".
- **§2.5 E-18** — desc "Lithic tokens only" → "card issuer-processor tokens only"; `issuer_card_ref` comment "Lithic card token" → "Card issuer-processor card token".
- **§3.1** — kyc/sessions "server creates Persona inquiry" → "server creates KYC inquiry".
- **§3.2** — share-purchase "`payment_token` (Stripe)" → "`payment_token` (payment processor)".
- **§3.12** — cases/evidence "Persona evidence pack" → "KYC evidence pack".
- **§3.14 webhooks table Source column** — Persona/Stripe/Lithic/Plaid → eKYC provider / Payment processor / Card issuer-processor / Account-linking provider.
- **§3.15** — US-10.2 "engine = E on Lithic settlement events" → "on card issuer-processor settlement events".
- **§4 intro** — rewrote the FALSE claim. Was: "Vendor selections follow the decision log … substituting an equivalent later is an integration change, not a requirements change." Now: providers are procurement decisions (TBD), role-only; eKYC ХУР/XYP candidate noted; explicitly states the prior claim was false because vendor names had leaked into schema fields and error codes, and that this run removes that leakage so future substitution CAN be an integration change.
- **§4.1** heading "Persona — eKYC" → "eKYC / Identity-Verification Provider"; added Provider=TBD + ХУР/XYP line; all Persona mentions in flow/failure prose → "the eKYC provider".
- **§4.2** heading "Plaid — …" → "Account-Linking Provider — …"; added Provider=TBD; "Plaid Link", "Plaid outage" → account-linking-flow / account-linking-provider outage.
- **§4.3** "settlement … via NETC (Lithic-issued cards)" → "(cards issued by the card issuer-processor)".
- **§4.4** heading "Stripe — …" → "Payment Processor — …"; added Provider=TBD; "Stripe Elements" → "the payment processor's client SDK"; "debit Stripe clearing" → "debit payment-processor clearing"; "Stripe outage" → "a payment-processor outage".
- **§4.5** heading "Lithic — …" → "Card Issuer-Processor — …"; added Provider=TBD + EP-5 entity-gated note; all Lithic mentions → the card issuer-processor / issuer-processor.
- **§5.1** "(Lithic-hosted, PCI-DSS scope reduction)" → "(issuer-processor-hosted, …)".
- **§5.2 SLA table** "(Lithic webhook, end-to-end)" → "(card issuer-processor webhook, end-to-end)".
- **§5.5 minimization** "KYC media held by Persona … PAN held by Lithic" → "KYC media held by the eKYC provider … PAN held by the card issuer-processor".

## 3. Confirmations

- **No rail term touched** — ACH+ / Banksüljee / RTGS / NETC counts unchanged; verify rails=20 (= baseline).
- **No monetary amount touched** — 10,000₮ / 1,000,000 minor-unit par values unchanged; verify usd=131 (= baseline).
- **No non-vendor field name touched** — only the 5 vendor-named identifiers + 5 webhook paths in the rename table were renamed. `processor_token_ref` and `issuer_card_ref` left as-is (already neutral).
- **No vendor invented** — every concrete provider is marked a procurement decision (TBD); eKYC additionally carries the ХУР/XYP compliant-alternative candidate note. Honesty rule respected.

## 4. Deliberately left in place (with reason)

- **E-signature provider ("Dropbox Sign or DocuSign class", §1.1 / §4.6)** and **notification vendors ("SendGrid-class" email, "Twilio-class" SMS, §1.1 / §4.7)** — these fall OUTSIDE the brief's vendor scope (Stripe / Plaid / Lithic / Persona / Jumio only), are already framed as `-class` role abstractions rather than committed selections, are not in the canonical rename table, and are not counted by `verify-docs.sh`'s vendor check. Left untouched to stay within T1 scope. Flag for a follow-up run if full third-party-vendor neutralization is later desired.
- **Generic word "vendor"** (e.g. "vendor webhooks", "vendor call", "vendor outage", `502 VENDOR_UNAVAILABLE`, "≤ 30 s of vendor completion") — no named vendor; left as neutral generic language.
- **DEC-5 definition** lives in `01_business_analysis.md` (§6, ~01:361), not in 04 — its amendment is a separate task per the brief. 04's references to DEC-5 keep the ID; only the vendor names around them were removed.

no code executed
