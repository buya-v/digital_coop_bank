# T2 — Vendor removal in `01_business_analysis.md`

**Branch:** `softhouse/T2-vendors-01`
**Scope:** `idea-lab/final_requirements/01_business_analysis.md` ONLY. No code executed; no build/deploy/tests run.
**Brief:** `.softhouse/vendor-removal-brief.md`. Direction is CLAUDE.md-ratified (US vendors are market-invalid) — this executes it, not a new decision.

## Result
- `.softhouse/verify-docs.sh` → **VERDICT: PASS**, all HARD checks **0** (unchanged).
- Vendor DRIFT unchanged at 83 (= baseline, no increase). The script's vendor pattern (`persona (inquiry|webhook|api|returns|watchlist)|via persona`) never matched 01's prose forms ("vendor: Persona", "via the Persona verification service", "the Persona integration"), so removing them leaves the numeric count flat; the sharp drop is a whole-run (01–05) expectation driven by 04's identifiers. HARD staying 0 and drift not increasing is the T2 bar and both hold.
- Only Persona/Jumio appeared as vendors in 01 — no Stripe/Plaid/Lithic prose in this file.
- 10 changed lines total (5 edits, each a remove+add). No rail term (ACH/ACH+/FedNow/SEPA/RTGS/Banksüljee/NETC/threshold) and no monetary amount (₮/MNT/$/5,000,000/10,000) appears in any added or removed line — confirmed by filtering the diff to `^[+-]` lines. No invented Mongolian vendor.

## DEC-5 — before / after (line ~361, normative)

**BEFORE** (Topic: *KYC vendor*):
> **Persona** is the canonical identity-verification vendor for document OCR, biometric selfie match, and watchlist screening. All specs, sequence diagrams, and integration stories reference Persona. | Sprint docs alternated between Persona and Jumio. | One vendor must be named for consistent API contracts and test fixtures. Persona is selected for its developer-first API … (Jumio remains a documented fallback candidate; substituting it later is **an integration change, not a requirements change**.)

**AFTER** (Topic: *eKYC provider selection*; ID and meaning = KYC provider selection preserved):
- Provider defined **by role** — document OCR, liveness/selfie match, watchlist (sanctions/PEP) screening — **not by vendor name**; concrete provider is a **procurement decision (TBD)**. Specs reference the role ("the eKYC provider") and neutral identifiers (`kyc_inquiry_id`, `/webhooks/kyc`).
- **ХУР/XYP state-register lookup named as the compliant-alternative candidate**, because biometric eKYC (face-match + liveness) is **legally unresolved** — CLAUDE.md blocking-question 2 (Personal Data Protection Law may restrict biometric collection to enumerated state bodies).
- Superseded Variants column now records the removed names (Persona canonical, Jumio fallback) and the removed false claim.
- Rationale **corrects the false claim**: vendor substitution *was* a requirements change (not "an integration change") because vendor names had leaked into schema field names (`persona_inquiry_id`) and API error codes; this migration makes the fields/codes vendor-neutral **so that going forward** provider selection/substitution *can* become an integration/procurement change. States meaning unchanged; only the named vendor removed.

## Every other vendor prose ref changed (old → new)

1. **CAP-1.2 capability tree (~156)**: `eKYC & Identity Verification (document OCR, biometric selfie match — vendor: Persona; see DEC-5)` → `… (document OCR, liveness/selfie match, screening — provider is a procurement decision, TBD; see DEC-5)`
2. **F-101 feature description (~252)**: `automated sanctions/PEP screening via the Persona verification service` → `automated sanctions/PEP screening via the eKYC / identity-verification provider (document OCR, liveness, screening; provider is a procurement decision, TBD — see DEC-5)`
3. **DEC-19 rationale (~375)**: `Downstream onboarding stories and the Persona integration need one status vocabulary.` → `… and the eKYC-provider integration need one status vocabulary.`
4. **Canonical Term Table (~390)**: term `**Persona (vendor)**` / def "The identity-verification vendor (DEC-5)." / Do-NOT-Use "Jumio (except in the fallback note)" → term `**eKYC provider**` / def "The identity-verification provider — document OCR, liveness, screening; a procurement decision (TBD) per DEC-5." / Do-NOT-Use "any named vendor (Persona, Jumio); \"the KYC vendor\" as though one is named".

## Intentionally retained
- Line ~246 `Persona references use the canonical IDs from Section 1.` — this is the **user-persona** noun (P-1…P-5), not a vendor; left unchanged (verify-docs excludes user-persona forms).
- Persona/Jumio names still appear in DEC-5's *Superseded Variants* and the term table's *Do NOT Use* columns — the correct, honest place to record what was removed.
- DEC-5 references elsewhere (as the ID) — none needed changing in 01 beyond the definition.

## Out of scope (untouched, per brief)
- Files 00, 02, 03, 04, 05, 06 — a separate run / follow-up handles 04's identifier renames and the rest.
- Backend code, payment-rail terms, monetary amounts.
