# T4 — Vendor removal in 03_acceptance_criteria.md

Branch: `softhouse/T4-vendors-03`
Scope: `idea-lab/final_requirements/03_acceptance_criteria.md` only, per `.softhouse/vendor-removal-brief.md`.

## Vendor prose refs changed (old → new), with context

1. L27 — "Persona is the identity-verification vendor for document OCR, biometric selfie match, and sanctions/PEP screening (DEC-5)." → "The eKYC / identity-verification provider (procurement decision, TBD) performs document OCR, biometric selfie match, and sanctions/PEP screening (DEC-5)."
2. L69 (heading) — "### US-1.2 — eKYC Verification via Persona (Document, Biometric, Screening) (L)" → "### US-1.2 — eKYC Verification via the eKYC Provider (Document, Biometric, Screening) (L)"
3. L75 — "Persona OCR extracts the DEC-6 name/address fields" → "the eKYC provider's OCR extracts the DEC-6 name/address fields"
4. L85 — "Persona liveness detection fails the capture" → "the eKYC provider's liveness detection fails the capture"
5. L90 — "Persona returns a confirmed document-fraud signal" → "the eKYC provider returns a confirmed document-fraud signal"
6. L100 — "containing the Persona evidence bundle" → "containing the KYC evidence bundle"
7. L176 — "(Persona re-check or US-12.1 admin review, per policy)" → "(eKYC re-check or US-12.1 admin review, per policy)"
8. L479 — "[ADJUDICATED → ... DEC-37 ...: Plaid instant verification primary; micro-deposit fallback for unsupported institutions]" → "...: account-linking-provider instant verification primary; micro-deposit fallback for unsupported institutions]"
9. L615 — "[ADJUDICATED → ... DEC-40 ...: REJECTED — no platform-side expiry rule; defer to the issuer-processor (Lithic) default]" → "...defer to the card issuer-processor's default]"
10. L1587 — "`KycStatus = PENDING_REVIEW` and the Persona evidence bundle attached" → "`KycStatus = PENDING_REVIEW` and the KYC evidence bundle attached"

Total: 10 refs (matches the ~10 estimated in the task). Stripe was not referenced in this file (no card/wallet-purchase collection scenario names a vendor here) — nothing to neutralize for it.

## Identifiers renamed

None. Searched for every identifier in the brief's rename table (`persona_inquiry_id`, `plaid_item_ref`, `plaid_public_token`, `PLAID_INSTANT`, `502 PLAID_UNAVAILABLE`, `/webhooks/persona`, `/webhooks/plaid`, `/webhooks/stripe`, `/webhooks/lithic`, `processor_token_ref`, `issuer_card_ref`) — none appear in `03_acceptance_criteria.md`. Those field/webhook names live in `04_technical_architecture.md` (out of scope for T4).

## DEC-5 amendment

Not applicable to this file — DEC-5's normative definition lives in `01_business_analysis.md` §6 (a different task's scope per the brief). `03` only *references* DEC-5 by ID (kept unchanged) while its accompanying prose (item 1 above) was neutralized to match the eventual amended DEC-5 (eKYC provider, procurement/TBD).

## Confirmed untouched

- No monetary amounts changed (verify-docs.sh DRIFT `USD amounts` = 131, unchanged from baseline).
- No rail terms changed (`ACH+` reference at L177 area retained verbatim; DRIFT `US payment rails` = 20, unchanged from baseline).
- No non-vendor field name touched; no Mongolian vendor invented — all replacements are role-neutral ("the eKYC provider", "the account-linking provider", "the card issuer-processor") or generic ("KYC evidence bundle").

## verify-docs.sh result

```
HARD (must be zero): all 5 checks PASS at 0.
DRIFT: USD amounts 131 (= baseline); US payment rails 20 (= baseline);
       US vendor references 79 (-4 from baseline 83).
VERDICT: PASS
```

No code executed.
