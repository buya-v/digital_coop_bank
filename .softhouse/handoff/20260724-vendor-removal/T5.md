# T5 — Vendor removal in `02_user_stories.md`

**Branch:** `softhouse/T5-vendors-02`
**Scope:** ONLY `idea-lab/final_requirements/02_user_stories.md`. No code, build, deploy, or tests run.
**Gate:** `.softhouse/verify-docs.sh` — HARD stays **0** (all five checks PASS); vendor DRIFT **83 → 82 (-1 from baseline)**; USD **131 = baseline** (unchanged); rails **20 = baseline** (unchanged).

Marking: [VERIFIED] = confirmed by grep/gate output in this run.

## Vendor prose refs changed (old → new + context)

All four vendor mentions in this file were `Persona` (Stripe/Plaid/Lithic do not appear in `02_user_stories.md` — confirmed by grep). Per the brief's mapping (Persona/Jumio → "the eKYC / identity-verification provider"), and DEC-5 (amended elsewhere, in `01_business_analysis.md`, out of scope for this task) naming the concrete provider a procurement decision (TBD). No Mongolian vendor invented.

| Line | Old | New | Context |
|---|---|---|---|
| 49 | `### US-1.2 — eKYC Verification via Persona (Document, Biometric, Screening)` | `### US-1.2 — eKYC Verification via the eKYC Provider (Document, Biometric, Screening)` | Story title. |
| 52 | `Integration with the Persona verification service (DEC-5) for document capture with OCR extraction into the DEC-6 name/address fields, ...` | `Integration with the eKYC / identity-verification provider (DEC-5; provider is a procurement decision, TBD) for document capture with OCR extraction into the DEC-6 name/address fields, ...` | US-1.2 description — vendor named + TBD note added. |
| 509 | `Queue management for onboarding escalations from US-1.2 (review Persona evidence, approve → APPROVED, or reject → REJECTED with reason codes) ...` | `Queue management for onboarding escalations from US-1.2 (review KYC evidence, approve → APPROVED, or reject → REJECTED with reason codes) ...` | US-12.2 description ("Persona evidence" → "KYC evidence", per brief's "Persona evidence pack → KYC evidence pack" mapping). |
| 607 | `| F-101 | Instant Digital Onboarding & eKYC | US-1.1, US-1.2 | Journey vs. Persona verification split |` | `| F-101 | Instant Digital Onboarding & eKYC | US-1.1, US-1.2 | Journey vs. eKYC-provider verification split |` | Feature → Story Coverage Table note. |

## Deliberately left unchanged

- **Line 10** — `**Personas:** referenced only by canonical ID (P-1 ... P-5)` — this is the requirements-persona noun (P-1..P-5 user personas), not the Persona vendor. Left untouched; correctly excluded by `verify-docs.sh`'s own vendor regex (which only counts `persona (inquiry|webhook|api|returns|watchlist)` / `via persona`, not the bare noun).
- **Lines 142, 583, 657** — `Savings Goals (Personal Pots)`, `Personal Impact Scorecard` (×2) — substring match on "Persona" inside "Personal"; not a vendor reference. No change.
- **Line 56 INVEST note** — `"vendor-integration story"` — generic term, does not name a specific vendor (Stripe/Plaid/Lithic/Persona/Jumio). Left as-is; out of the brief's rename scope.
- **`processor_token_ref` / `issuer_card_ref`** — not present in this file (they live in `04_technical_architecture.md`, out of scope for T5).

## Only 1 of 4 changes affects the gate's vendor DRIFT counter

`verify-docs.sh`'s vendor regex (`\b(stripe|plaid|lithic)\b|persona (inquiry|webhook|api|returns|watchlist)|via persona`) only matches the literal phrase `via Persona` (line 49, the story title). The other three edits (lines 52, 509, 607) neutralize the vendor mention in prose that the gate's narrow regex didn't count in the first place, but they are still required by the brief's "neutralize vendor mentions in story descriptions" instruction and are included for completeness/consistency with `01`'s DEC-5 amendment. Gate: vendor 83 → 82.

## DEC-5

DEC-5's normative definition lives in `01_business_analysis.md` §6 and its amendment (naming the eKYC provider TBD + correcting the "integration change, not requirements change" claim) is **out of scope for T5** — a separate task owns `01`. This file only references `DEC-5` by ID (line 52), which is correct per the brief ("DEC-5 REFERENCES elsewhere stay as 'DEC-5'; only the definition changes").

## Confirmed out of scope / untouched

- **No rail term changed** — `ACH+`/`RTGS`/`Banksüljee`/`NETC` terminology not touched (rails DRIFT gate = 20 = baseline, unchanged).
- **No monetary amount changed** — USD gate = 131 = baseline, unchanged; no ₮ figures touched.
- **No non-vendor field name touched** — `KycStatus`, `KYC evidence`, story/epic/feature/capability IDs, table structure, and headers all preserved verbatim.
- Docs `00`, `03`, `04`, `05`, `06` — not edited (other tasks' scope).
- Backend code — not touched (separate follow-up run per the brief).

## Verification

```
$ .softhouse/verify-docs.sh
HARD (must be zero): all 5 PASS (0 each)
DRIFT: usd 131 (=baseline), rails 20 (=baseline), vendor 82 (-1 from baseline 83)
VERDICT: PASS
```

`git diff --stat` confirms only `idea-lab/final_requirements/02_user_stories.md` changed, 4 lines modified (8 lines in unified diff: 4 removed + 4 added), no other file touched.

no code executed
