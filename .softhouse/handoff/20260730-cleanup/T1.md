# T1 — Documentation-consistency cleanup (terminology only)

**Branch:** `softhouse/T1-cleanup` (worktree `agent-aa720d097eebdc9fe`)
**Scope:** `idea-lab/final_requirements/` only. No code executed. No amount/rail/vendor-name RE-migration.
**Files touched:** `01_business_analysis.md`, `04_technical_architecture.md`, `05_prd_and_roadmap.md`.
**Untouched (as mandated):** `06_ledger_addendum.md`, `07_ledger_audit_verdict.md`.

## Verify (`.softhouse/verify-docs.sh`)
Before and after — **VERDICT: PASS**. HARD all 0. DRIFT unchanged at baseline:
usd=131 (=), rails=15 (=), vendor=6 (=). No DRIFT increase.

---

## (1) etsgiin_ner spacing (#11)
Canonical field name is `etsgiin_ner` (underscore) per DEC-6 / ORM / OpenAPI.

| old → new | location |
| :-- | :-- |
| `ovog/etsgiin ner/ner` → `ovog/etsgiin_ner/ner` | 01_business_analysis.md:256 (F-105 feature row) |

- Denoted the FIELD triple alongside `ovog`/`ner`; corrected to underscore. Sole space-form occurrence in the corpus; `grep 'etsgiin ner'` now returns 0.

## (2) Other-vendor abstraction (#18)
Named e-signature / notification vendors neutralized to role abstractions, consistent with the DEC-5 provider-neutrality pattern (provider = procurement decision, TBD; no Mongolian vendor invented).

| old → new | location |
| :-- | :-- |
| `**E-signature provider** (Dropbox Sign or DocuSign class) \| Legally binding … (US-6.6).` → `**E-signature provider** \| Legally binding … (US-6.6). Concrete provider is a procurement decision (TBD).` | 04_technical_architecture.md:32 (external-systems table) |
| `FCM/APNs (push), SendGrid-class email, Twilio-class SMS — behind …` → `FCM/APNs (push), an email provider, and an SMS provider — behind … Concrete email/SMS providers are a procurement decision (TBD).` | 04_technical_architecture.md:33 (external-systems table) |
| `dispatches (FCM/APNs push, SendGrid-class email, Twilio-class SMS)` → `dispatches (FCM/APNs push, email via the notification provider, SMS via the notification provider)` | 04_technical_architecture.md:1105 (§4.7 flow) |
| `**E-signature vendor unnamed** ("Dropbox Sign or DocuSign class") while all other integrations are named vendors → procurement …` → `**E-signature provider unnamed** — defined by role (per the DEC-5 provider-neutrality pattern), concrete provider TBD → procurement …` | 05_prd_and_roadmap.md:409 (§6.3 inconsistency #8) |

Note on 05:409: the original clause "while all other integrations are named vendors" was made false by the earlier DEC-5 vendor-neutralization pass (integrations are now role-defined). Reframed to the DEC-5 pattern rather than left contradictory — still terminology-scoped, no requirement changed. The item's substance (procure/name the e-sign provider before S3, US-6.6) is preserved.

`FCM/APNs` deliberately retained: these are the mobile push transports (Firebase Cloud Messaging / Apple Push Notification service), platform primitives not a procurement choice, and not in the #18 target set.

### Deliberately LEFT (with reason)
| occurrence | location | why left |
| :-- | :-- | :-- |
| "Named **Persona** as the canonical vendor, with **Jumio** as a fallback candidate" | 01_business_analysis.md:361 | This is the DEC-5 **old-value / migration-record column**, whose purpose is to document exactly which US vendors were removed. Neutralizing the names would falsify the migration record (same principle as the #15 historical-quote exception). Not a live spec reference. Not counted by the vendor DRIFT regex. |
| "any named vendor (Persona, Jumio); …" | 01_business_analysis.md:390 | Canonical-glossary **anti-pattern guard** column: it lists Persona/Jumio as examples of forbidden vendor names authors must NOT use. The names are instructive (what to avoid); removing them weakens the guard. Descriptive prose, not a live reference. |

After edits, `grep 'Jumio'` returns only these two record/guard rows; `grep -iE 'docusign|dropbox sign|sendgrid|twilio'` returns 0.

## (3) 'cents' → 'minor units' (#15, docs only, outside 06)
MNT's minor unit is möngö (obsolete); convention is whole-tögrög, so a neutral "minor units" is correct.

| old → new | location |
| :-- | :-- |
| `**MNT, integer minor units (cents)**` → `**MNT, integer minor units**` | 04_technical_architecture.md:9 (conventions) — the "(cents)" gloss was a US-ism; a literal "(minor units)" would be redundant with the preceding "minor units", so the stale parenthetical was dropped rather than duplicated. |
| `share.par_value_cents` → `share.par_value_minor_units` | 04_technical_architecture.md:657 (E-55 config-key example) |
| `roundup.batch_threshold_cents` → `roundup.batch_threshold_minor_units` | 04_technical_architecture.md:657 (E-55 config-key example) |

Both config keys are illustrative examples in the E-55 ConfigurationParameter registry; grep confirms no other reference to either key. `grep -i cents` outside 06 now returns 0. No "cents" left outside 06 to flag as a protected US-context quote.

---

## Confirmations
- No amount re-migration (USD DRIFT 131 unchanged), no rail re-migration (rails 15 unchanged), no vendor-name re-migration of the five main vendors (vendor 6 unchanged).
- `06_ledger_addendum.md` and `07_ledger_audit_verdict.md` NOT touched (`git status` shows only 01/04/05 modified).
- Headers and DEC-n/US-n/EP-n/E-n IDs preserved.
- No code executed.
