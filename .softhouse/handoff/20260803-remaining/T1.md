# T1 — Migrate `06_ledger_addendum.md` to Mongolia (audit verdict HIGH #2)

**Branch:** `softhouse/T1-06-migrate` (worktree `agent-a109e4a91c1dfa7f2`; committed, NOT pushed).
**Scope:** `idea-lab/final_requirements/06_ledger_addendum.md` only. No code executed. Design-doc
consistency pass only. `06` KEEPS its do-not-implement / pending-controller-ratification framing.

## Framing / invariants preserved
- Version line updated to mark the Mongolia migration but restated **"still do-not-implement —
  pending controller ratification (`07`)"**. Status line ("Blocks S1 implementation") unchanged.
- **§3 hold formula UNTOUCHED** — `available_balance = balance + signed_sum(MEMO_HOLD entries on the
  account)` and the AUDIT CORRECTION block (lines ~149–164) are not in the diff. The only §3 edit is
  the L-3 paragraph rail-name "ACH returns" → "ACH+ returns" (naming, not the formula).
- **§6.4 dividend LOGIC UNTOUCHED** — Step-1/3/4/5/6 pseudocode (`distributable_pool`, `N(m)`,
  `weight_k_bps`, largest-remainder) not in the diff. Only currency naming (`$0.00`→`0₮`) and the
  Subchapter-T legal flag changed in §6.4.
- All invariants (L-1…L-7), section numbers, IDs (DEC-/US-/EP-/E-/LA-), and the accounting logic
  (double-entry, posting balance, sign conventions, precision model) preserved. No renumbering.

## Changes by category

### Currency (USD → MNT, DEC-18)
- §5.1: "signed 64-bit integers in **USD minor units (cents)**" → "**MNT minor units**". Dropped
  "cents" as the minor-unit term everywhere.
- Precision naming: "micro-cents"→"micro-units", `μ¢`→`μ`, `pool_¢`→`pool_minor`,
  `eod_balance_¢`→`eod_balance_minor`, `posted_¢`→`posted_minor`, `accumulated_μ¢`→`accumulated_μ`,
  §8.6 `accrued_μ¢`→`accrued_μ`, "residual cent"→"residual minor unit", "few cents"→"few minor units".
- **Par $25.00 → 10,000₮** (provisional, pending DEC-11 / legal — noted on first use in §2.3; also
  §4.6 "multiple of 10,000₮", §6.5). Par restated as a US-12.5 config seed, never hard-coded.
- **§6.5 reinvestment worked example RE-DERIVED (not scaled):** `84,600₮ at 10,000₮ par →
  ⌊84,600/10,000⌋ = 8 shares issued, 4,600₮ residual to savings` (matches the task's target).
- **§6.2 interest worked check RE-DERIVED:** `500,000₮ @ 2.00%` → exact annual 10,000₮
  (1,000,000 minor units); twelve floored monthly postings total **9,999.99₮**, ~**0.0099₮**
  (≈1 minor unit) carried in the accumulator and posting in January. Verified: daily accrual
  `round_half_even(2,000,000,000,000/365·... )` rounds down for this principal, so the cross-year
  residual carry (the pedagogical point) is preserved and value is conserved (L-5).
- **§5.5** quote aligned to migrated `03` EP-3: `"$1,000.00 × 2.00%/365"` → `"1,000,000₮ × 2.00%/365"`.
- **§4.5** truncation example: `$500` → `500,000₮`, and the "roughly 3–5%" claim recomputed HONESTLY —
  at ordinary MNT balances the per-day minor-unit remainder is immaterial (a 500,000₮ balance loses a
  few tögrög/year), but it grows without bound as a share of interest for the smallest savers and L-5
  forbids discarding value at any balance. (The USD "3–5%" figure did not survive re-denomination; the
  accumulator/monthly-posting design conclusion is unchanged and still justified by L-5.)
- Zero entitlements `$0.00` → `0₮` (§6.4 Step 2 and degenerate case).
- §11 changelog item 4 de-USD-ed ("cents-only"→"minor-unit-only").

### Chart of accounts (§2)
- `1010 Stripe Clearing` → **`1010 Payment-Processor Clearing`** (role-neutral; provider TBD/procurement).
- `1020 ACH Clearing` → **`1020 ACH+ Clearing`** (24/7).
- `1030 Card Network Settlement Clearing` → **`1030 NETC Card Settlement Clearing`**.
- `1040 Wire Clearing` → **`1040 Banksüljee (RTGS) Clearing`** (outbound large-value >₮5,000,000).
- `1050 RTP / FedNow Clearing` → **RETIRED** (Mongolia has no separate real-time rail; ACH+ is 24/7
  and covers the instant case, so former real-time activity clears through 1020). Documented in a note
  under the §2.1 table (precedent: the deleted 5300 account). §1.2 range "1010–1050"→"1010–1040",
  "ACH operator"→"settlement operator". §7.3 clearing-aging "10xx–105x"→"10xx–104x".
- §2.4 `4100 Fee Income`: "Wire fees" → "RTGS (Banksüljee) transfer fees".
- §2.5 `5400`: "Stripe/processor fees" → "Payment-processor fees".
- No Mongolian vendor NAME invented.

### Posting rules (§4.3 external rails) — reconciled to `04` E-8 (`EXTERNAL_ACH_PLUS_IN |
EXTERNAL_ACH_PLUS_OUT | RTGS_OUT`)
- `EXTERNAL_ACH_OUT` → **`EXTERNAL_ACH_PLUS_OUT`** (hold → origination → settlement; 1020 ACH+
  Clearing; hold now posts to **9030** not "9000-class").
- `EXTERNAL_ACH_IN` → **`EXTERNAL_ACH_PLUS_IN`** (receipt + settlement; 1020 ACH+ Clearing).
- `WIRE_OUT` → **`RTGS_OUT`** — the ADDED large-value (>₮5,000,000) external-out rule. Given the
  standard outbound three-stage shape mandated by §4.3's own intro (**hold → 9030 → origination →
  settlement**) plus the optional transfer-fee leg carried over from WIRE_OUT.
- `RTP_IN` / `RTP_OUT` → **dropped** (fold into the ACH+ types; no separate real-time rail).
- Added the amount-based routing rule (>₮5m → RTGS_OUT / ≤ → ACH+), threshold = US-12.5 config
  (Governor's order), never hard-coded.
- Returns: "ACH return codes populate failure_reason" → return **reason** populates it; the return-code
  **catalogue is the settlement operator's, config/TBD** pending Bank-of-Mongolia settlement-agent
  selection (NOT invented — honesty rule).
- §4.4 cards: `1030 Card Settlement Clearing` → `1030 NETC Card Settlement Clearing` (all legs).
- §4.7 `1020 ACH Clearing` → `1020 ACH+ Clearing` (BACKING_DISBURSEMENT, SURPLUS_MATCH_DISBURSEMENT);
  §4.9 RECOVERY same. §4.1 share collection/remittance/refund → `1010 Payment-Processor Clearing`.

**RTGS_OUT balances (each stage a balanced Dr/Cr pair, L-1):**
- request accepted (hold): Dr Member TXN / Cr 9030 External-Payment Holds (`MEMO_HOLD`).
- origination: Dr Member TXN / Cr 1040 Banksüljee (RTGS) Clearing (hold released).
- settlement: Dr 1040 Banksüljee (RTGS) Clearing / Cr 1000 FBO Cash.
- fee (if configured): Dr Member TXN / Cr 4100 Fee Income.
- Note: giving RTGS_OUT a hold stage is application of §4.3's own "every outbound rail = hold →
  origination → settlement" rule (WIRE_OUT lacked it, a residual Draft-2 inconsistency), and uses the
  new 9030 account per the task. Balanced; L-1 satisfied at every stage.

### Memo account (§2.6)
- Added **`9030 External-Payment Holds`** — counter-account for `EXTERNAL_ACH_PLUS_OUT` and `RTGS_OUT`
  outbound `MEMO_HOLD` pairs. §4.3 external-out holds now post to 9030 instead of overloading
  `9000 Card Authorization Holds` (which stays card-auth-specific).

### Law (US-law framings flagged, NOT re-invented)
Each marked **`[NEEDS Mongolian-law grounding: FRC / Bank-of-Mongolia …]`** — no Mongolian law/citation
fabricated (the marked flag is the correct answer):
- §6.2 + LA-7: **Reg DD / TISA** deposit-disclosure framing.
- §4.10 + LA-15: **Reg E** dispute / provisional-credit framing.
- §6.4 + LA-1: **Subchapter T** cooperative-patronage-tax framing (kept the substantive concern that
  governance-as-patronage may be impermissible; formula still supports weight 0).

## Gate — `.softhouse/verify-docs.sh` (06 is NOT in META_DOCS; must genuinely pass)

```
HARD (must be zero):
  PASS  US deposit-insurance regime (FDIC/NCUA)    0
  PASS  stale MNT 3m RTGS threshold                0
  PASS  first_name / last_name                     0
  PASS  float / double / DECIMAL money types       0
  PASS  hardcoded UTC+8                            0

DRIFT (must not increase — Mongolia migration in progress):
  PASS  USD amounts                                117  (-14 from 131)
  PASS  US payment rails                           1  (-14 from 15)
  PASS  US vendor references                       0  (-6 from 6)

VERDICT: PASS
```

06's own contribution to all three drift counters is now **0** (USD, rails, vendor) — verified by the
exact gate rails logic (`grep -rEoh 'ACH\+|\b(ACH|FedNow|SEPA)\b' 06 | grep -vxF 'ACH+'` → empty) and
by direct `$`/vendor scans. The changelog/notes describe from-states without gate-counted literals
(no `$`+digit, no Stripe/Plaid/Lithic, no bare `ACH`/`FedNow`). Baseline not edited (verifier
re-baselines centrally).

## Confirmations
- Accounting LOGIC + §3 hold formula + §6.4 dividend logic: **untouched** (verified via targeted diff
  greps — those formula lines are absent from the diff).
- RTGS_OUT rule: **balances** at every stage (Dr = Cr, L-1).
- Do-not-implement / pending-controller-ratification framing: **preserved**.

no code executed
