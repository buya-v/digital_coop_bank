# T1 — EP-2 share-purchase (US-2.1) build-readiness doc

## Doc created

`docs/ep2-share-purchase-readiness.md` — verified inventory + acceptance-map + ratification split for EP-2/US-2.1 (`PENDING_PAYMENT → ACTIVE`). Analyst-only; no code written, no contract/model/requirements files edited. `06_ledger_addendum.md`'s `SHARE_SUBSCRIPTION` posting rule and L-4 are reproduced (not implemented) in a boxed §3, explicitly labelled "DO NOT IMPLEMENT until U1."

## Key verified facts (with file:line)

- Contract has **10** EP-2 ops (not just the 4 named), all read from `backend/openapi/paths/ep2-shares.yaml:9-317`; `Idempotency-Key` is required on `purchaseMembershipShare` (:18-24) and (INFERRED-per-contract) `createClosureRequest` (:167-175).
- `require_active_member` (`backend/app/auth/deps.py:132-157`) **already exists**, already gives uniform 403 for SUSPENDED/PENDING_* with no status leakage, and is **not yet wired into any router** — it's the exact mechanism US-2.2 S2 needs.
- No share/ledger wiring exists anywhere in `app/api/routers` or `app/services` (grep confirmed, comment-only hits).
- `MembershipShare` model (`backend/app/models/membership.py:105-121`) is missing `subscription_transaction_id`/`redemption_transaction_id` FKs that `04:182` requires for registry↔ledger reconciliation.
- `Account` model (`backend/app/models/ledger.py:55-71`) has no chart-of-accounts columns (`system_account_code` etc.) that `06 §2/§8.1` requires — expected, since `06` is do-not-implement, but flagged since the §3 posting box references accounts (1010/1000/5400/3000) with no representable row today.
- `03_acceptance_criteria.md` has zero literal occurrences of `AMOUNT_MISMATCH` despite `05:53` claiming it's one of the "16 scenarios" — a real doc-inconsistency (G-3 in the doc).
- `ShareStatus.MEMBER = "ISSUED"` (`membership.py:40-42`) is a name/value mismatch unlike every other enum in the codebase; flagged as [UNVERIFIED] risk since I did not execute code to confirm SQLAlchemy's default `Enum` serialization behavior (respecting the "no code executed" boundary).

## The split (summary)

- **Buildable now**: par-value config validation, Idempotency-Key plumbing, DEC-4 transition guard, `require_active_member` wiring, payment-initiation port+mock, `getSharePurchaseStatus`/`getMyMembership`/`listMyShares` read paths, `createEligibilitySnapshot`.
- **Ledger-gated (needs U1)**: the `SHARE_SUBSCRIPTION` posting itself, activation-on-settlement, `SHARE_REDEMPTION`, L-4 enforcement, `equity_ledger_reconciliation`, the `NONZERO_BALANCES`/`LOCKED_GUARANTEE_PLEDGE` closure blockers.

Full file-by-file mapping is in the doc §4.

## Gaps found

Six concrete, sourced findings (G-1 through G-6) in doc §5: two model↔04 schema gaps (missing FKs), one doc-inconsistency (05 overclaims 03's AC coverage of AMOUNT_MISMATCH), one AC-premise ambiguity (US-2.1 S4's PENDING_KYC state is never durably reachable given current kyc.py), one AC↔contract error-code mismatch (409 vs 422 for illegal transition), and one flagged-but-unverified enum serialization risk.

## Unverified / could-not-confirm

- G-6 (ShareStatus enum serialization) — source facts verified, runtime behavior explicitly marked [UNVERIFIED] since no code was executed per this task's boundary.
- Whether `getClosurePreconditions`'s non-`NONZERO_BALANCES` blockers are truly ledger-independent depends on how EP-6/EP-3 end up modelling loan/pledge/pot status, which I did not read in full (out of this task's file list) — flagged as a caveat in doc §4.1's last row rather than asserted.

## Follow-ups

- When schema work starts: apply `04:182`'s two FK columns to `MembershipShare`, and `06 §8.1/§8.2/§8.3`'s chart-of-accounts amendments to `ledger.py` (this is a future coder task, not done here).
- Someone (PO or the eventual builder) should resolve G-4 (US-2.1 S4's premise) and G-5 (409 vs 422) before writing tests against those scenarios literally.
- A future coder should add a `values_callable` to `ShareStatus`'s `Enum(...)` column (or otherwise confirm/fix G-6) the first time `MembershipShare` rows are actually written.

## Verification

`bash .softhouse/verify-docs.sh` → PASS, all DRIFT counts unchanged (usd=117, rails=1, vendor=0) — `docs/` is outside the scanned `idea-lab/final_requirements/*.md` set, so no regression possible from this file by construction.

no code executed
