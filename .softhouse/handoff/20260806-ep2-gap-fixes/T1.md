# T1 — EP-2 gap-fix DOC-consistency edits (G-3, G-4, G-5)

Branch: `softhouse/T1-ep2-gap-docs` (forked from main; main untouched).
Scope: three DOC-consistency fixes aligning `03`/`05` to the ratified OpenAPI contract and the built member-auth architecture. No application code. No DEC changed.

## Changes Made

### G-3 — SETTLEMENT-amount-mismatch scenario added to `03` US-2.1 (+ `05` recount)

New numbered scenario inserted after US-2.1 Scenario 3 (idempotency); the former Scenario 4 (API-bypass) renumbered to Scenario 5 and rewritten under G-4 (see below).

**Before** (`03` US-2.1 ended at Scenario 4 = the API-bypass scenario, immediately after Scenario 3):
```
**Scenario 4 — Negative (security): API bypass attempt before KYC approval**
* **Given** a client authenticated to an application with `KycStatus = PENDING_REVIEW` (so `MembershipStatus = PENDING_KYC`)
...
```

**After** — new Scenario 4 (settlement mismatch):
```
**Scenario 4 — Negative (settlement): settled amount ≠ par is rejected**
* **Given** a `PENDING_PAYMENT` application initiated a 10,000₮ (1,000,000 minor units) membership-share purchase
* **When** the settlement webhook reports a settled amount ≠ 10,000₮ (≠ 1,000,000 minor units)
* **Then** the settlement is rejected with `422 AMOUNT_MISMATCH` (per the payment-processor settlement webhook contract, `04 §4.3`)
* **And** no entry posts to the Membership Share Account
* **And** `MembershipStatus` stays `PENDING_PAYMENT`
* **And** the mismatch is written to the audit log for reconciliation. (Amounts are compared as integer minor units, ₮-denominated; no partial or wrong-amount share is ever posted.)
```

`05` EP-2 AC-themes line:
- **Before:** `**AC themes (03 EP-2, 16 scenarios):** amount must equal configured par (`422 AMOUNT_MISMATCH`);`
- **After:**  `**AC themes (03 EP-2, 17 scenarios):** amount must equal configured par (`422 AMOUNT_MISMATCH`);`

The `422 AMOUNT_MISMATCH` parenthetical in `05` now traces to a real `03` scenario (US-2.1 Scenario 4).

### G-4 — API-bypass scenario premise corrected (now `03` US-2.1 Scenario 5)

**Before:**
```
**Scenario 4 — Negative (security): API bypass attempt before KYC approval**
* **Given** a client authenticated to an application with `KycStatus = PENDING_REVIEW` (so `MembershipStatus = PENDING_KYC`)
* **When** it POSTs directly to the share-purchase endpoint
* **Then** the API responds `403 Forbidden` (authenticated but state-ineligible)
* **And** no payment is initiated and no ledger entry is created
* **And** the attempt is written to the audit log as a policy violation.
```

**After:**
```
**Scenario 5 — Negative (security): purchase attempt by a member not in `PENDING_PAYMENT`**
* **Given** an authenticated Member whose `MembershipStatus` is not `PENDING_PAYMENT` (e.g. already `ACTIVE`, or `SUSPENDED`)
* **When** they POST directly to the share-purchase endpoint (member-authenticated via `get_current_member`)
* **Then** the API responds `403 Forbidden` (authenticated but state-ineligible)
* **And** no payment is initiated and no ledger entry is created
* **And** the attempt is written to the audit log as a policy violation
* **And** a pre-auth `PENDING_KYC` onboarding application cannot reach this status guard at all: it holds only a resume token, not a member JWT (a Member is minted only at KYC approval → `PENDING_PAYMENT`, DEC-4), so it is rejected at authentication (`401`), never at the status check.
```

Security intent preserved (state-ineligible caller → `403`, no payment, no ledger entry, audit-logged); no assertion weakened. The unreachable premise (a PENDING_KYC pre-auth application presenting member auth) is replaced with a reachable one and its unreachability is stated explicitly.

### G-5 — illegal-transition code reconciled to the contract (`03` US-2.2 Scenario 3)

**Before:**
```
* **Then** the domain layer rejects the transition with `409 Conflict` and an "illegal transition" error naming the from/to states
```

**After:**
```
* **Then** the domain layer rejects the transition with `422 ILLEGAL_TRANSITION` (matching the admin status-transition contract, `04 §3.2`) and an "illegal transition" error naming the from/to states
```

Rest of the scenario (from/to state names, "no partial state change occurs") intact. Aligned to the contract's declared code; the OpenAPI contract was NOT edited.

## Scenario recount

- **Old EP-2 count (`05`):** 16 scenarios.
- **New EP-2 count (`05`):** 17 scenarios.
- **How counted:** counted `**Scenario` bullets between the `### US-2.1` header and the `## EP-3` section boundary in `03`. Per story after edits: US-2.1 = 5 (was 4; G-3 added one), US-2.2 = 4, US-2.3 = 4, US-2.4 = 4 → 5+4+4+4 = **17**. The prior baseline of 16 was 4+4+4+4. G-4 renumbers (does not add); G-5 changes a code (does not add). Only G-3 changes the count, by +1. [VERIFIED: `awk`/`grep` count = 17]

## Decisions

- **G-4 kept `403`, not `409`.** The task specified `403`, and the share-purchase op declares BOTH `403` (Forbidden) and `409 WRONG_MEMBERSHIP_STATE` [VERIFIED: backend/openapi/paths/ep2-shares.yaml:48-58]. `403` is contract-declared for this op, so preserving the original scenario's `403` is contract-consistent. Reconciling `403`-vs-`409` selection is out of this task's scope (G-4 is a premise fix), so I did not change the code.
- **G-5 aligned to `422 ILLEGAL_TRANSITION`.** No principled reason found for `409` on the illegal-transition case: the contract's analogous admin status-transition op explicitly declares `422 ILLEGAL_TRANSITION` [VERIFIED: backend/openapi/paths/ep2-shares.yaml:310-311], while `409` on that op is reserved for `PENDING_TRANSITION_EXISTS` (a concurrency conflict, not an illegal transition) [VERIFIED: ep2-shares.yaml:304-305]. So `422` is unambiguously correct; aligned per the "contract is single source of truth" rule.
- **No ratified DEC touched.** DEC-4 (status machine) and DEC-11 (par value) unchanged; edits align prose/error-codes to already-ratified decisions and the contract.

## Sources

- `422 AMOUNT_MISMATCH` on the settlement webhook (SETTLEMENT side): [VERIFIED: backend/openapi/paths/webhooks.yaml:118-121 — "422 AMOUNT_MISMATCH — settled amount != configured par value ... (04 §4.3)"].
- `422 AMOUNT_MISMATCH` on the share-purchase request op (REQUEST side, already covered by US-2.1 preamble): [VERIFIED: backend/openapi/paths/ep2-shares.yaml:59-60].
- `422 ILLEGAL_TRANSITION` is the contract's declared illegal-transition code: [VERIFIED: backend/openapi/paths/ep2-shares.yaml:310-311].
- Share-purchase op is member-authenticated and returns `403`/`409` for state-ineligible callers: [VERIFIED: backend/openapi/paths/ep2-shares.yaml:40-58].
- `get_current_member` = member JWT verification (RS256), unknown/CLOSED subject → uniform `401`: [VERIFIED: backend/app/auth/deps.py:85-129].
- Onboarding stays pre-auth (resume token, not member JWT); an applicant is not yet a member: [VERIFIED: backend/app/auth/deps.py:1-6; backend/app/api/routers/onboarding.py:20-33].
- Member minted only at KYC approval → `PENDING_PAYMENT`; REJECTED never creates a Member (DEC-4): [VERIFIED: backend/app/services/kyc.py:226-270; backend/app/api/routers/onboarding.py:20-21].
- Non-ACTIVE authenticated statuses (SUSPENDED/PENDING_PAYMENT/PENDING_KYC) are reachable member states behind auth: [VERIFIED: backend/app/auth/deps.py:132-157].
- verify-docs baseline (`.softhouse/baseline.txt`): usd=117, rails=1, vendor=0.

## Unverified

- None material. All error codes, the auth architecture, and the scenario count are [VERIFIED] against the contract, backend code, or a direct count. The `04 §3.2`/`04 §4.3` section cross-references in the new prose mirror the section labels the OpenAPI descriptions themselves cite (ep2-shares.yaml / webhooks.yaml), not an independent read of `04`.

## Blockers

- None. No fix required changing a ratified DEC (DEC-4 status machine, DEC-11 par value), so no STOP condition was triggered.

## Follow-ups

- **`403`-vs-`409` for state-ineligible share purchase (non-blocking).** US-2.1 Scenario 5 asserts `403`; the contract also declares `409 WRONG_MEMBERSHIP_STATE` for "not in PENDING_PAYMENT" on the same op. Both are declared, so no contradiction, but a future pass could pin down which the endpoint returns for an already-`ACTIVE`/`SUSPENDED` caller and make `03` name exactly that code. Out of scope here (G-4 was a premise fix, not a code reconciliation).
